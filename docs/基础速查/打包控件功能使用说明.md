# 打包控件功能使用说明

本文档列出项目中已封装好、可复用的控件和模式。仓库地址：
`ui/custom_widget/` — `__init__.py` 统一导出，一行 `from ui.custom_widget import …` 即可使用。

## 全控件速查表

| 导出名 | 是什么 | 一句话说明 | 源文件 |
|--------|--------|-----------|--------|
| `ConfigCheckBox` | `QCheckBox` | 自动设置 `objectName='ConfigCheckBox'` → 主题样式自动生效 | `checkbox.py` |
| `AlignmentChecker` | `QCheckBox` | 一端勾上不能取消（对齐互斥用） | `checkbox.py` |
| `QFontChecker` | `QCheckBox` | 字体选择用复选框（语义别名） | `checkbox.py` |
| `ClockDial` | `QWidget` | PS 式指针拨盘，角度 + 半径距离 | `clock_dial.py` |
| `ColorSwatchBtn` | `QPushButton` | 色块按钮，`setColor()`/`color()` + `colorChanged` | `color_button.py` |
| `ColorPickerDialog` | `QDialog` | 完整取色器（HSV 面板 + 调色板 + 屏幕吸色 + HEX/RGB 一键复制） | `color_picker.py` |
| `ComboBox` | `QComboBox` | 未聚焦时禁用滚轮（防误触长列表） | `combobox.py` |
| `ConfigComboBox` | `ComboBox` | 自适应宽度的配置面板下拉框 | `combobox.py` |
| `ParamComboBox` | `ComboBox` | 绑定模块参数 key，发射 `paramwidget_edited` 信号 | `combobox.py` |
| `SizeComboBox` | `QComboBox` | 可编辑 + 数字验证 + `setValue()`/`changeByDelta()` | `combobox.py` |
| `SmallComboBox` | `ComboBox` | 同 `ComboBox`，更小样式 | `combobox.py` |
| `SmallSizeComboBox` | `SizeComboBox` | 同 `SizeComboBox`，更小样式 | `combobox.py` |
| `FlowLayout` | `QLayout` | 自动换行的流式布局（支持动画） | `flow_layout.py` |
| `GroupFrame` | `QFrame` | 1px 半透明圆角边框，分组容器 | `group_frame.py` |
| `FadeLabel` | `QLabel` | 渐隐动画标签（1200ms → 透明） | `label.py` |
| `ColorPickerLabel` | `QLabel` | 点击打开 `ColorPickerDialog` | `label.py` |
| `SmallColorPickerLabel` | `ColorPickerLabel` | 同上，更小 | `label.py` |
| `ClickableLabel` | `QLabel` | 发射 `clicked()` 信号 | `label.py` |
| `ConfigClickableLabel` | `ClickableLabel` | 配置面板风格的可点击标签 | `label.py` |
| `CheckableLabel` | `QLabel` | 点击切换 `checked_text`/`unchecked_text` | `label.py` |
| `TextCheckerLabel` | `QLabel` | 点击高亮背景（主题色），互斥用 | `label.py` |
| `ParamNameLabel` | `QLabel` | 配置面板参数名（预设字号 + 半透明背景） | `label.py` |
| `SmallParamLabel` | `QLabel` | 同上，不设字号（用默认字体） | `label.py` |
| `SizeControlLabel` | `QLabel` | 拖拽调大小，发射 `size_ctrl_changed(delta)` | `label.py` |
| `SmallSizeControlLabel` | `QLabel` | `SizeControlLabel` 小号版 | `label.py` |
| `MessageBox` | `QMessageBox` | 自动管理信号/槽注册 | `message.py` |
| `FrameLessMessageBox` | `QMessageBox` | 无边框消息框 | `message.py` |
| `TaskProgressBar` | `QWidget` | 进度条 + 描述文字 + 可选 ETA | `message.py` |
| `ProgressMessageBox` | `QDialog` | 无边框模态带 `TaskProgressBar` | `message.py` |
| `ImgtransProgressMessageBox` | `ProgressMessageBox` | 四管线进度条（检测/OCR/修复/翻译） | `message.py` |
| `NoBorderPushBtn` | `QPushButton` | 无边框按钮 | `push_button.py` |
| `ExpandingToolButton` | `QPushButton` | 可展开工具按钮 | `push_button.py` |
| `ConfigScrollBar` | `QScrollBar` | 8px 圆角，悬停动画（全局统一） | `scroll_bar.py` |
| `ScrollBar` | `QWidget` | 旧版 Fluent 风格滚动条（渐变消失），预留给特殊场景 | `scrollbar.py` |
| `NoArrowsSpinBox` | `QSpinBox` | 无箭头整数输入，主题感知圆角 | `spinbox.py` |
| `NoArrowsDoubleSpinBox` | `QDoubleSpinBox` | 无箭头浮点输入，同风格 | `spinbox.py` |
| `ConfigLineEdit` | `QLineEdit` | 单行文本输入，与 `NoArrowsSpinBox` 同风格 | `text_input.py` |
| `ConfigTextEdit` | `QTextEdit` | 多行文本输入，同风格 | `text_input.py` |
| `ConfigSectionHeader` | `QWidget` | 配置面板加粗章节标题 | `section_header.py` |
| `PaintQSlider` | `QSlider` | 双气泡：描述文本固定于滑块顶部居中，数值气泡随手柄移动（MD3 风格），`setValueFormat()` 自定义文本 | `slider.py` |
| `RangeSlider` | `QWidget` | 双滑块范围选择，拖动/悬停手柄时显示对应数值气泡 | `slider.py` |
| `ExpandLabel` | `QWidget` | 可折叠面板标题栏（带 chevron 箭头） | `view_panel.py` |
| `PanelArea` | `QScrollArea` | 可滚动画板区域 + 展开/折叠 | `view_panel.py` |
| `PanelAreaContent` | `QWidget` | `PanelArea` 内容容器，发射 `after_resized()` | `view_panel.py` |
| `PanelGroupBox` | `QWidget` | 卡片式分组容器 | `view_panel.py` |
| `ViewWidget` | `QWidget` | 标题 + 内容容器，注册到全局展开/折叠管理 | `view_panel.py` |
| `SeparatorWidget` | `QFrame` | 水平分隔线（`HLine` + `Sunken`） | `widget.py` |
| `Widget` | `QWidget` | `WA_StyledBackground` 基类，使 CSS `background-color` 生效 | `widget.py` |

**工具函数：**

| 导出名 | 是什么 | 一句话说明 |
|--------|--------|-----------|
| `isDarkTheme()` | 函数 | 返回当前深色模式布尔值 |
| `themeColor()` | 函数 | 返回当前主题主色 `QColor` |
| `borderColor()` | 函数 | 返回当前主题 `@borderColor` `QColor` |
| `widgetBackgroundColor()` | 函数 | 返回当前主题 `@widgetBackgroundColor` `QColor` |
| `combobox_with_label()` | 工厂函数 | 创建 `SmallComboBox`/`ComboBox` + `label` 对 |
| `pick_screen_color()` | 函数 | 屏幕吸色管：全屏覆盖 + 8x 放大镜，左键取色、右键/Esc 取消（冻结帧采样，事件驱动不卡 UI） |

## 样式生效机制：全局兜底 vs 类名选择器

`config/stylesheet.css` 有两套生效机制，决定了"写 Qt 原生类会不会掉样式"：

**1. 全局类型选择器兜底** —— 写 Qt 原生类就自动生效，无需封装类：
`QPushButton`、`QRadioButton`、`QMenu`、`QTabBar`、`QScrollBar`（所有滚动条）、
`QListWidget`/`QTreeView`（含 item 选中态）、`QToolTip`、`QSplitter`、`QProgressBar`，
以及 `QComboBox`/`QLineEdit`/`QPlainTextEdit` 的基础款（直角边框）。这些随手写也不会错。

**2. 类名/objectName 选择器** —— 只匹配封装类名，写原生类会**静默掉样式**
（不报错、能跑，只是和全应用风格不一致）。输入类控件必须用封装类：

| 场景 | 必须用 | 原生写法的后果 |
|------|--------|---------------|
| 下拉框 | `ui/custom_widget/combobox.py::ConfigComboBox` / `ui/custom_widget/combobox.py::ParamComboBox` / `ui/custom_widget/combobox.py::SmallComboBox` | 得到基础款直角样式，与圆角输入风格不符；且无防滚轮误触 |
| 数字/浮点输入 | `ui/custom_widget/spinbox.py::NoArrowsSpinBox` / `ui/custom_widget/spinbox.py::NoArrowsDoubleSpinBox` | **完全无 QSS 规则** → Windows 原生箭头，深色主题下突兀 |
| 单行输入 | `ui/custom_widget/text_input.py::ConfigLineEdit` | 只有基础款边框，非圆角输入款 |
| 多行输入 | `ui/custom_widget/text_input.py::ConfigTextEdit` | `QTextEdit` 无任何全局规则（全局规则只覆盖 `QPlainTextEdit`）→ 完全原生外观 |
| 复选框 | `ui/custom_widget/checkbox.py::ConfigCheckBox`（自动设 objectName） | 指示器走原生渲染（`QDialog` 内有 catch-all 兜底，对话框外没有） |
| 分组/容器/分隔线 | `ui/custom_widget/group_frame.py::GroupFrame` / `ui/custom_widget/widget.py::SeparatorWidget` / `ui/custom_widget/widget.py::Widget` | 无边框无圆角 |

**原则：新建输入类控件一律 `from ui.custom_widget import …`，不要直接实例化
`QComboBox`/`QSpinBox`/`QLineEdit`/`QTextEdit`。** 封装类名即 QSS 选择器名，
可对照 `config/stylesheet.css` 确认样式是否命中。

**目视工具**：`scripts/style_showcase.py`（仓库根目录运行）——Tab1 原生 vs
封装对照、Tab2 全部可离线实例化控件分区展示。新增控件时在该脚本对应分区
rows 列表追加一行 `(说明, 工厂)` 即可，无法离线展示的在文件尾部 EXCLUDED
注释登记原因。

## 核心模式

### 模式 A：ConfigSubBlock 禁用自动变灰

`ConfigSubBlock`（`ui/configpanel.py::ConfigSubBlock`）的 `changeEvent` 在禁用时自动将 `name_label` 文字变灰（`@disabledForegroundColor`），启用时恢复。

```python
# 禁用 — label 自动变灰，子控件 Qt 自带禁用渲染
some_sublock.setEnabled(False)
```

无需额外写 stylesheet。`?` 帮助按钮不受影响。

### 模式 B：ConfigContent 内控件禁用 CSS

`config/stylesheet.css` 限定 `ConfigContent` 域的禁用态样式（下划线 + 灰色）自动生效：

```css
ConfigContent QLineEdit:disabled {
    border-bottom: 1px solid @disabledForegroundColor;
    color: @disabledForegroundColor;
}
```

### 模式 C："—" 占位符

禁用数值字段时用 "—" 替代数值，表明"不适用"：

```python
field_widget.blockSignals(True)
field_widget.setText("—" if not enabled else restore_value)
field_widget.blockSignals(False)
```

必须 `blockSignals` 防止 textChanged 触发持久化。

### 模式 D：主题色取值

`get_theme_color(key)`（`ui/misc.py`）从当前主题解析任意 `@variable` 颜色：

```python
from ..misc import get_theme_color
success = get_theme_color(key="@successColor")
label.setStyleSheet(f"color: {success.name()};")
```

常用 key：`@successColor` / `@warningColor` / `@dangerColor` / `@accentPrimary` / `@borderColor`。该函数自动切换 dark/light 主题。

## 新增原则

1. 优先考虑能否在现有类中通过 `changeEvent` / CSS 等方式自动处理
2. 若无法自动，再考虑辅助方法或独立函数
3. 新控件添加到 `ui/custom_widget/`，在 `__init__.py` 导出，并在上表追加一行
4. 同步在 `scripts/style_showcase.py` 对应分区 rows 追加展示行（无法离线展示的在 EXCLUDED 注释登记）
