# 上游 (BallonsTranslator) UI 参考

> 上游仓库：`/d/ruanjian/BallonsTranslator`
> 记录日期：2026-07-12

## 可复刻功能清单（按优先级排列）

### P0 — 已确认移植

| # | 事项 | 来源 | 状态 |
|---|------|------|------|
| 1 | 模块切换线程化（`ModuleThread` + 可取消 + 依赖检测） | `module_manager.py` | ✅ 2026-07-12 |
| 2 | BottomBar 模块选择器（`ModuleSelectionWidget` + 图标） | `module_tool_button.py` | ✅ 2026-07-12 |
| 3 | Outside Click 自动隐藏设置面板 | `configpanel.py` | ✅ 2026-07-12 |
| 4 | TreeView 导航（父子结构展开折叠） | `configpanel.py` | ✅ 2026-07-12 |

### P1 — 已确认移植

| # | 事项 | 来源 | 状态 |
|---|------|------|------|
| 5 | 管线拆分为独立页面（当前为合并页面） | `configpanel.py` | ✅ 2026-07-12 |
| 6 | LLM 配置独立页面（工具栏 + 内联表单，原 dialog 布局搬入页面） | `configpanel.py`, `profile_manager.py` | ✅ 2026-07-12 方案 A 实施 |

### P2 — 已同步（样式/尺寸）

| # | 事项 | 说明 |
|---|------|------|
| 7 | checkbox indicator 样式 | 12px 圆角方框 + SVG 勾选图标，见 `checkbox_checked.svg` |
| 8 | 输入框下划线聚焦效果 | ConfigContent 内 `border-bottom: 1px solid` + 聚焦变蓝 |
| 9 | BottomBar 模块按钮样式 | `QToolButton#BottomBarModuleToolButton` |
| 10 | QMenu 菜单样式 | 圆角 + 悬停高亮 |
| 11 | 控件尺寸紧凑化 | comboBox height 26px、width 180/300/420 |

---

## 样式迁移记录

### 2026-07-12 — 首次迁移

**来源文件：**
- `BallonsTranslator/resources/stylesheet.css`
- `BallonsTranslator/resources/themes.json`
- `BallonsTranslator/resources/icons/checkbox_checked.svg`
- `BallonsTranslator/resources/icons/textdetect.svg`
- `BallonsTranslator/resources/icons/text.svg`
- `BallonsTranslator/resources/icons/eye.svg`
- `BallonsTranslator/resources/icons/image.svg`

**改动内容：**
见本次 commit 的 `config/stylesheet.css` 和新增图标文件。

⚠️ **样式覆盖不完全** — Checkbox、ConfigContent、BottomBar 等模块的部分子控件/状态仍有未覆盖的样式缺口，需后续完整逐项排查应用（对比 upstream stylesheet.css 逐段审对）。

## P0/P1 实现记录（2026-07-12）

四个 P0/P1 事项同日落地，逐文件的改动明细见当时的 commit：

- **P0#1 模块切换线程化**：`ui/module_manager.py` 的 `ModuleThread` 加 `cancel_event`（`threading.Event`）与三阶段准备进度（importing → instantiating → loading_model），`ui/custom_widget/message.py` 的 `ProgressMessageBox` 加可选停止按钮。
- **P0#2 底部栏模块选择器**：新增 `ui/module_tool_button.py::ModuleSelectionWidget`（QToolButton + 18px SVG 图标 + QMenu，展开时从隐藏 `SmallComboBox` 重建菜单项、当前项打 ✓），替代旧 `SelectionWithConfigWidget` 组合框；未引入上游的 LLM profile 子菜单。
- **P1#5 管线拆分**：`ui/configpanel.py` 导航树 Modules 下拆为 5 个独立条目，各页面独立滚动区。
- **P1#6 LLM 配置独立页面（方案 A 内联表单）**：`utils/profile_manager.py` 建 `ProfileManagerWidget`（顶部工具栏 + 全宽表单 + 自动保存）。**该方案已被取代**——`ProfileManagerWidget`/`ProfileManagerDialog` 于 2026-09-09 删除，LLM Profile 页改为卡片式（`ui/llm_profile_cards.py`），见下方 v1.5.15 批次。

### 2026-09-09 — 上游 v1.5.13 → v1.5.15 吸收批次

**背景：** 用户审查卡片页后指出「单供应商切模型体验不如上游」，并要求顺带评估上游最近版本的改动。调研窗口 v1.5.13..v1.5.15（dev = 1.5.15，本地克隆原只到 1.5.13，本次 fetch 补齐）。用户定取向：**底层性能/实现优先服从上游**（不自造轮子），显性功能按需求取舍。

**上游模型切换机制（`ballontranslator/ui/llm_profile_widgets.py` + `ballontranslator/utils/llm_profiles.py`）：**

- 每个 profile 持久化 `model_options` / `vision_model_options` / `image_model_options`，`PROVIDER_DEFAULTS` 给每个供应商预置 2~11 个模型名，`_merge_profile_options` 把「内置 + 已保存 + 当前值」合并去重；
- 摘要行常驻模型下拉（`ProfileCardWidget` 的 summary grid，`ParamComboBox` + `+`/`−` 就地增删），展开前就能切；
- 底部模块栏 `ballontranslator/ui/module_tool_button.py::ModuleSelectionWidget._buildProfileMenu` 按分区列出各 profile 的模型清单，点一下同时切 profile + 模型；
- **上游没有 Fetch Models**（全文搜过），靠预置清单 + 手填覆盖离线切换。

**fork 采纳与偏差：** 吸收「清单 + 摘要行下拉 + 手填增删」，但**不预置供应商模型表**（用户拍板：清单只由 Fetch Models 或手填产生）；保留并升级 fork 的 Fetch Models 为多选添加（`utils/profile_manager.py::FilterableListDialog` 的 `multi_select`），网络只用于刷新清单。底部模块栏模型子菜单未做（fork 用模块参数里的 `active_profile` 而非上游的 `pcfg.module.*_llm_id`，留待后续）。

**已移植的底层修复：**

| 上游提交 | 内容 | fork 落点 |
|---|---|---|
| `9b34135` + `e88c655` | 瓦片取整余量、tile tier 随缩放分层；整块 staging 超光栅策略时改逐瓦片裁剪，大窗口/大半径下效果不再整块消失 | `ui/text_engine/rendering/raster.py::plan_effect_raster`、`ui/text_engine/effects/renderer.py::_draw_tiled_effects` |
| `b36210b` | LaMa 预处理补边对齐替代重采样，保住网点与遮罩边缘（上游只删了配套测试，代码保留） | `modules/inpaint/base.py`（fork 无 `inpaint_default.py`） |
| `afad9f5` | 16-bit 灰度 PNG 取高字节归一 | `utils/io_utils.py::imread` |
| `73741cf` | `QDoubleValidator` 固定 C locale，小数点不受系统 locale 影响 | `ui/module_parse_widgets.py::ParamLineEditor` |
| `a86ebb1` | Ctrl+C/V/X 改 `QKeySequence.StandardKey` 匹配 | `ui/canvas.py`、`ui/text_engine/item.py` |
| `595f6fd` | 水平排版单个空格软换行不再跳动 | `ui/text_engine/horizontal_layout.py::_trailing_space_layout` |
| `5acc4f4` / `51f1760` / `2ecd5f7` | 画笔粗细对数映射 + 精确像素输入 | `ui/custom_widget/slider.py::Slider`、`ui/drawingpanel.py` |

**评估后不移植：** 合成粗体（用户不感兴趣，此前已决定删除该特性）、上游 LLM 上下文/记忆/摘要（`modules/context/*`、`ui/llm_context_editor.py`、`ui/bulk_page_summary_editor.py`、`trans_llm.py` 重写）、全页 LLM OCR、Photoshop Bridge、HayaiOCRv2、韩语/匈牙利语 ts、`ui/module_manager.py` 的 LLM 错误对话框重构——均依赖上游 `LLMProfile` dataclass / id 主键 / SecretStore，或与本 fork 方向相反。上游 `200ccc1`（`QFontDatabase.font()` 空 style 崩溃）在 fork 无同型调用，不适用。
