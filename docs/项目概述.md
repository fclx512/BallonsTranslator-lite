# BallonsTranslator-lite 项目概述

> 合并自：原 `README.md`（文档索引）+ 原 `项目概述.md`（架构介绍）+ `AGENTS.md`（开发速查）
> 最后更新：2026-08-14

目标读者：无法直接访问代码库的 AI 助手及新加入的开发者。
目的：了解本项目全貌，快速上手开发。

---

## 一、这是什么项目

一款用于翻译漫画/图片的桌面应用程序。管线包含五个阶段：

**文字检测 → OCR → 翻译 → 图像修复 → 文字渲染**

基于 **PyQt6** 构建，支持 Windows/macOS/Linux。

本分支是减法导向的 fork：精简优先，不添加未被要求的功能；交互路径越短越好。

## 二、技术栈

| 层级 | 技术 |
|-------|-----------|
| GUI 框架 | PyQt6（通过 `qtpy` 兼容层） |
| 语言 | Python 3.10+ |
| 样式 | CSS 样式表（`config/stylesheet.css`），支持 `@variable` 占位符，运行时解析 |
| 动画 | `QTimer` 驱动的手动插值，封装于 `ui/overlay_modal.py`（`OverlayModal` 淡入淡出）和 `ui/overlay_slide.py`（`OverlaySlider` 侧滑），缓动函数 `InOutExpo`，帧率由 `pcfg.animation_fps` 控制 |
| 异步 | 基于 `QThread` 的工作线程，用于 LLM 调用和管线执行 |
| 国际化 | Qt Linguist：`self.tr("English")` → `.ts` → `.qm` |

## 三、应用结构

```
BallonsTranslator-lite/
├── launch.py                      入口点
├── launch.bat                     统一启动脚本（自动检测 Python/GPU/更新模式）
├── modules/                       管线模块（启动时通过装饰器自注册）
│   ├── base.py                    BaseModule、模块发现、设备检测
│   ├── prepare_local_files.py     本地预下载模型文件校验
│   ├── textdetector/              文字检测（ctd / yolov5 / paddlev6 / ysg）
│   ├── ocr/                       OCR（mit48px / mit48px_ctc / llm_api / onnx / none）
│   ├── translators/               翻译（llm_api / sakura）+ 上下文批处理 / hooks
│   ├── inpaint/                   图像修复（lama / aot / ffc / patchmatch / none）
│   ├── context/                   LLM 上下文管理（errors / glossary / history / token_usage）
│   └── glossary_extractor.py      术语表提取
├── utils/
│   ├── config.py                  ProgramConfig 数据类、加载/保存
│   ├── proj_imgtrans.py           项目管理（页面、文字块、撤销栈）
│   ├── textblock.py               核心数据单元（坐标、原文、译文、字体、遮罩）
│   ├── textblock_mask.py          文字块蒙版生成
│   ├── registry.py                模块注册装饰器模式
│   ├── structures.py              nested_dataclass / Config / Dict 基类
│   ├── shared.py                  路径常量（PROGRAM_PATH、CONFIG_PATH 等）
│   ├── profile_manager.py         LLM API 配置（翻译器/OCR 共用）
│   ├── ai_tools.py                AI 工具执行
│   ├── version.py                 从 pyproject.toml 读取版本号
│   ├── proj_compact.py            项目序列化（用于 LLM 上下文）
│   ├── font_mapping.py / fontformat.py    字体检测与映射
│   ├── psd_exporter.py / psd_binary_exporter.py / psd_binary_writer.py / psd_jsx_exporter.py / psd_descriptor.py / psd_engine_data.py / psd_packbits.py  PSD 导出
│   ├── text_layout.py / text_processing.py / textlines_merge.py  排版工具
│   ├── imgproc_utils.py / io_utils.py / download_util.py    图像 I/O 与下载
│   ├── update_cache.py / updater.py / mirror.py / network_mirrors.py     更新缓存 / 核心更新器 / 镜像
│   ├── message.py / exceptions.py / lock.py / logger.py / debug_log.py / safe_qt.py     基础设施
│   ├── merger.py / split_text_region.py / stroke_width_calculator.py / shortcut_conflicts.py  杂项工具
│   ├── text_alignment.py / text_normalize.py / point_alignment.py     对齐与标准化
│   ├── package.py / package_installer.py / core_requirements.py       依赖安装
│   ├── lazy_registry.py / registries.py   延迟注册表
│   └── reasoning_params.py / env_diagnostic.py   推理参数与诊断
├── ui/
│   ├── mainwindow.py              主窗口
│   ├── mainwindowbars.py          标题栏 / 左侧栏 / 底部栏
│   ├── configpanel.py             配置面板（QStackedWidget 内部分页）、快捷键编辑
│   ├── overlay_modal.py           OverlayModal — 中心淡入/淡出模态（设置面板载体）
│   ├── overlay_slide.py           OverlaySlider — 滑入/滑出动画辅助工具（搜索、页码面板载体）
│   ├── canvas.py                  画布核心
│   ├── scene_textlayout.py        画布文字渲染
│   ├── textitem.py                画布文字项
│   ├── scenetext_manager.py       场景文字管理器
│   ├── io_thread.py               管线编排（检测 → OCR → 翻译 → 修复）
│   ├── text_panel.py              文本编辑面板
│   ├── textedit_area.py / textedit_commands.py    文本编辑区域与命令
│   ├── text_advanced_format.py / text_engine/    格式 / 效果（text_engine/ 含渲染、几何、变换）
│   ├── text_style_presets.py / fontstyle_manager.py        字体样式预设管理
│   ├── global_search_widget.py / page_search_widget.py     搜索功能
│   ├── update_checker.py          commit 版更新：UpdateThread + CommitUpdateDialog + AboutDialog
│   ├── update_thread.py / update_dialog.py   release 版更新：UpdateCheckThread + UpdateReleaseDialog
│   ├── theme_helpers.py           主题感知颜色辅助
│   ├── image_edit.py / drawingpanel.py / drawing_commands.py   绘图编辑
│   ├── module_manager.py / module_parse_widgets.py / module_tool_button.py  模块管理器
│   ├── dependency_dialog.py / model_check_dialog.py    依赖/模型检查
│   ├── network_settings_dialog.py / psd_export_dialog.py   设置对话框
│   ├── normalize_breaks_dialog.py / point_align_dialog.py  工具对话框
│   ├── glossary_dialog.py / glossary_extractor_dialog.py / context_menu_config.py  术语表与右键菜单
│   ├── pie_menu.py / pie_menu_editor.py      快捷环形菜单与编辑器
│   ├── misc.py / shared_widget.py / collapsible_section.py  共享 UI 工具
│   ├── merge_dialog.py / shadow_gradient_dialog.py          合并/阴影
│   ├── quick_symbol_dialog.py / fontformat_commands.py / fontstyle_manager_commands.py  其他对话框
│   ├── cursor.py / funcmaps.py / texteditshapecontrol.py ...    其他
│   ├── custom_widget/             自定义 widget 库（按钮、滑块、组合框等）
│   └── framelesswindow/           无边框窗口实现（Win/Linux）
├── config/
│   ├── stylesheet.css             全局样式表，含 @variable 占位符
│   ├── themes.json                主题定义
│   ├── custom_themes.json         用户自定义主题
│   ├── palette.json               调色板
│   ├── textstyles/                字体样式预设
│   ├── requirements_core.txt      核心依赖清单
│   └── config.json.example        配置模板（config.json 已 gitignore）
├── translate/
│   ├── zh_CN.ts                   中文翻译源文件
│   └── zh_CN.qm                   编译后的翻译文件
├── scripts/
│   ├── verify.py                  统一验证入口（语法 → 文档 → i18n → qm → 冒烟）
│   ├── check_docs.py / check_syntax.py / i18n_check.py / qm_compile.py   校验与 i18n 工具
│   ├── ts_auto_fill.py            自动同步 .ts 与源码
│   ├── build_portable.py          便携版打包（嵌入式 Python）
│   ├── generate_manifest.py / build_win.bat    清单与 Windows 构建
│   ├── download_models.bat / check_update.py 模型下载与更新
│   └── run_module.py / check_all.py          模块运行与检查
├── tests/
│   ├── test_startup_imports.py     启动导入冒烟
│   ├── test_proj_compact.py / test_psd_binary.py / test_psd_jsx_export.py   序列化 / PSD 导出
│   ├── test_text_transform_*.py / test_textblkitem_*.py   排版 / 文字块
│   ├── test_dependency_startup.py  依赖启动
│   └── ui/                         UI 测试
├── docs/                           本目录
│   ├── 项目概述.md                 本文档（项目架构 + 文档索引）
│   ├── daily_log.md                每日开发日志
│   ├── 基础速查/                   开发日常参考
│   └── 技术实现/                   难点技术记录
└── data/                           运行时数据（分词模型等）
```

## 四、文档索引

文档以中文为主。如需英文版，可借助 AI 翻译中文文档阅读。

### 基础速查（`docs/基础速查/`）

| 文件 | 说明 |
|------|------|
| `快捷键.md` | 快捷键系统架构与添加步骤 |
| `i18n.md` | 国际化（i18n）指南 |
| `新增设置项路线参考.md` | 新增配置项的完整实施路线（5 层参考） |
| `设置面板排版思路.md` | 设置面板两列表单（ConfigFormRow）排版约定 |
| `打包控件功能使用说明.md` | 打包控件功能使用说明与全控件速查表 |
| `配置导入导出.md` | 配置导入导出说明 |
| `依赖库说明.md` | 嵌入式 Python 环境（ballontrans_pylibs_win）说明 |
| `经验教训.md` | 开发经验教训与问题记录 |
| `上游参考.md` | 上游项目 UI 参考与样式迁移记录 |

### 技术实现（`docs/技术实现/`）

| 文件 | 说明 |
|------|------|
| `设置面板概述.md` | 设置面板架构、分页结构、动画机制、备注系统 |
| `排版技术.md` | 竖排标点对齐、縦中横、上游 PR#1238 文字变换调研 |
| `快捷键系统.md` | 快捷键系统技术实现 |
| `PSD导出_JSX路线.md` | PSD 导出 JSX 路线设计与问题记录 |
| `反向移植_完整流程.md` | fork→上游反向移植完整流程 |
| `反向移植_规范.md` | 反向移植规范（两仓库适配点） |
| `快捷菜单_实现总结.md` | 画布快捷菜单（环形 + 竖排）实现总结 |

### 其他

| 文件 | 说明 |
|------|------|
| `daily_log.md` | 每日开发日志 |

## 五、UI 架构模式

### 5.1 主窗口布局

`MainWindow` 采用侧边栏 + 主内容区布局：
- **左侧栏**：工具按钮（StateChecker — QCheckBox 子类，实现面板互斥切换）
- **中央**：画布区域，用于图片预览和文字编辑
- **右侧面板容器**：承载滑入面板（全局搜索、页码列表等）

### 5.2 滑入/淡入面板

浮动面板使用两种机制：

- **`ui/overlay_slide.py` 的 `OverlaySlider`**：从右侧边缘滑入（全局搜索、页码列表），固定宽度 480px，350ms 动画，`InOutExpo` 缓动函数，提供 `show()`、`hide()`、`resize()` API。
- **`ui/overlay_modal.py` 的 `OverlayModal`**：中心淡入/淡出模态（设置面板），scrim 压暗覆盖中央画布区，350ms `InOutExpo` 动画。

### 5.3 独立对话框

部分 UI 组件为独立的 `QDialog` 子类（非模态）：
- 配置面板的各种设置对话框（网络设置、配置文件管理、快捷键等）

这些组件使用标准 `QDialog` 布局，根布局为 `QVBoxLayout`。

### 5.4 Widget 组合模式

UI 类遵循以下模式：
1. `__init__` 调用 `_build_ui()` 组装所有 widget
2. 内部状态存储为实例变量（以 `_` 为前缀）
3. 信号在类级别定义，用于外部通信
4. 通过 `setObjectName()` 设置样式，与 stylesheet.css 中的 CSS 选择器匹配

示例：
```python
class MyPanel(QWidget):
    my_signal = Signal(list)

    def __init__(self, parent=None):
        super().__init__(parent)
        self.setObjectName("MyPanel")
        self._build_ui()

    def _build_ui(self):
        root = QVBoxLayout(self)
        # ... 构建子 widget ...
        btn = QPushButton(self.tr("Do Thing"))
        btn.setObjectName("MyActionBtn")
        btn.clicked.connect(self._on_action)
        root.addWidget(btn)
```

### 5.5 样式系统

`config/stylesheet.css` 中所有样式使用 `@variable` 占位符：
```css
#MyWidget {
    background-color: @widgetBackgroundColor;
    border: 1px solid @borderColor;
    border-radius: 6px;
    color: @qwidgetForegroundColor;
    font-size: 13px;
    padding: 8px;
}
```

变量由 `ui/misc.py` 中的 `parse_stylesheet()` 在运行时解析。
可用变量包括：`@widgetBackgroundColor`、`@emptyContentBackgroundColor`、
`@accentTranslate`、`@accentPrimary20`、`@borderColor`、`@successColor`、
`@dangerColor`、`@qwidgetForegroundColor`、`@inverseTextColor` 等。

约定：对象名称使用帕斯卡命名法，并带项目前缀，例如 `#AIReviewCard`、
`#AIChatPanel`、`#AIChangeReviewWindow`。

### 5.6 国际化

所有用户可见字符串必须使用 `self.tr("...")` 包裹：
```python
label = QLabel(self.tr("Accept All"))
```

例外情况：日志消息、LLM 提示词、字体预览字符串、语言映射字典。

## 六、关键设计约束

1. **禁止硬编码中文** — 所有 UI 文本通过 `self.tr()` 处理
2. **样式表变量** — 禁止硬编码颜色，始终使用 `@variable` 引用
3. **基于信号的解耦** — 数据/模型层不得导入 Qt widget
4. **`qtpy` 兼容** — 使用 `qtpy` 导入，而非直接 `PyQt6` 导入
5. **对象命名约定** — 帕斯卡命名法，带有有含义的前缀（AI、Config 等）
6. **配置持久化** — `pcfg` 是模块级单例；修改后必须显式调用 `save_config()`
7. **模块注册** — 管线模块通过装饰器自动注册，启动时发现

## 七、关键文件速查

| 文件 | 用途 |
|------|------|
| `launch.py` | 入口，命令行参数，PyTorch 设备 |
| `utils/proj_imgtrans.py` | 项目管理（页面、文字块、撤销栈） |
| `utils/textblock.py` | 核心数据单元（坐标、原文、译文、字体、遮罩） |
| `utils/config.py` | 配置读写 |
| `utils/shared.py` | 路径常量 |
| `utils/structures.py` | `nested_dataclass`，`Config`/`Dict` 基类 |
| `utils/profile_manager.py` | LLM API 配置管理（翻译器/OCR 共用） |
| `utils/ai_tools.py` | AI 辅助工具函数 |
| `ui/mainwindow.py` | 主窗口 |
| `ui/configpanel.py` | 配置面板、快捷键编辑 |
| `ui/text_panel.py` | 文本编辑面板 |
| `ui/io_thread.py` | 管线编排（检测→OCR→翻译→修复） |
| `ui/scene_textlayout.py` | 画布文字渲染 |
| `ui/overlay_modal.py` | `OverlayModal` — 中心淡入/淡出模态（ConfigPanel 用它） |
| `ui/overlay_slide.py` | `OverlaySlider` — 覆盖面板滑入滑出动画（搜索、分页用它） |
| `ui/custom_widget/` | 可复用控件库（`__init__.py` 统一导出） |
| `config/` | `config.json`(gitignore), `stylesheet.css`, `themes.json`, `custom_themes.json`, `textstyles/` |
| `scripts/` | `verify.py`, `check_docs.py`, `check_syntax.py`, `qm_compile.py`, `i18n_check.py` |

## 八、配置系统

每个管线阶段使用注册器模式（`utils/registry.py`），模块通过装饰器自注册：

```text
modules/
  textdetector/  →  detector_*.py  (@register_textdetector)
  ocr/           →  ocr_*.py       (@register_ocr)
  translators/   →  trans_*.py     (@register_translator)
  inpaint/       →  *.py           (@register_inpainter in inpaint/base.py)
  base.py        ←  BaseModule, 模块发现, 设备检测
```

`modules/base.py` 的 `init_module_registries()` 按文件命名模式扫描各目录，动态导入匹配的模块。

模块声明 `params: Dict` 自动渲染为 UI 表单。以 `_` 开头的 key 为内部参数，save/load 时保留。

全局配置 `pcfg`（`utils/config.py`）是模块级单例：
- 改 `pcfg` 后须显式调用 `save_config()`。仅 `closeEvent` 和 `ConfigPanel.hideEvent` 触发自动保存。
- 启动顺序：`launch.py` 先 `load_config()` 再 `init_module_registries()`。

## 九、测试流程

改代码后按以下顺序验证，避免遗漏回归。**首选统一入口**：

`python scripts/verify.py`

一条命令依次跑 语法 → i18n → qm → 冒烟；成功每步只打一行，失败才完整打印报错。各步自动判定：

- **语法**：只查 git 改动涉及的 .py（`--all` 改查全部 ui/+utils/）
- **i18n**：全量扫描；硬编码中文/缺失条目为失败，孤儿条目降级为警告（项目大量 `canvas.tr()`/`self.tr(variable)` 间接调用是已知噪音）
- **qm**：ts 有改动时自动编译
- **冒烟**：改动命中启动链文件（`launch.py`/`modules/base.py`/`utils/profile_manager.py`/`ui/configpanel.py`/`ui/mainwindow.py`）时自动触发，`--smoke` 可强制

需要时手动分步跑：

1. **语法检查**：`python scripts/check_syntax.py <文件...>`（支持多文件；查编译 + tab 字符 + UTF-8 BOM）
2. **i18n 检查**：`python scripts/i18n_check.py`；发版前 `--ci`；`--show-expected` 列出已知孤儿
3. **qm 编译**：`python scripts/qm_compile.py translate/zh_CN.ts translate/zh_CN.qm`
4. **启动冒烟测试**：`python tests/test_startup_imports.py`（单进程约 2s；模拟关键导入链，捕捉 `NameError` / `ImportError`）
5. **启动 app 目视确认**（可选，但推荐）：双击 `launch.bat` 或 `python launch.py`

## 十、快捷键系统

定义在 `ui/configpanel.py` 的 `DEFAULT_SHORTCUTS`/`_ACTION_NAMES`，`_SHORTCUT_GROUPS` 分组。安装/刷新见 `ui/mainwindow.py` 的 `_install_shortcuts()`/`refreshShortcuts()`。用户配置持久化在 `pcfg.shortcuts`（`config.json`）。详见 `docs/基础速查/快捷键.md`。

## 十一、动画系统

`ui/overlay_modal.py` 的 `OverlayModal`（中心淡入/淡出 + 压暗 scrim，scrim 仅覆盖 `centralStackWidget`；ConfigPanel 用它，duration 350ms, easing `InOutExpo`，`pcfg.animation_fps<0` 跳过）与 `ui/overlay_slide.py` 的 `OverlaySlider`（侧滑滑入；GlobalSearchWidget、PageList 用它）。`MainWindow` 中分别为 ConfigPanel（`_configModal`）、GlobalSearchWidget、PageList 各创建一个实例。`StateChecker`（`QCheckBox` 子类）实现 LeftBar 面板互斥切换。ConfigPanel 现为**内部分页**（`QStackedWidget`），导航树 `ConfigTable` 点击切页而非滚动；子 `QDialog` 打开时经 `_run_modal_dialog` 暂停 backdrop 点击。

## 十二、UI 样式参考

现有 UI 使用以下视觉模式：
- **卡片**：圆角（6-8px）、边框、内边距 8-12px
- **按钮**：扁平带边框、悬停背景色变化、高度 26-36px
- **滚动区域**：细滚动条、无边框
- **文字**：基础字号 13px，标签启用自动换行
- **间距**：兄弟 widget 之间 6-8px，容器外边距 12px
- **分隔线**：细 QFrame 线条，高度 1-2px

---

## 未来 / 计划工作

### 发版打包与更新器

面向非技术用户的稳定版发布管线。便携分发采用**嵌入式 Python**（`scripts/build_portable.py` 构建 `ballontrans_pylibs_win/`），通过 GitHub Releases 分发；增量更新仅替换 `modules/`、`utils/`、`ui/`、`launch.py` 等源码，不替换 Python 环境。

**已实现：**
- **release 版更新**（用户侧）：`utils/updater.py`（`BallonsTranslatorUpdater`：比对 `fclx512/BallonsTranslator-lite` 最新 release、下载 source zip、原子替换源码）+ `ui/update_thread.py`（`UpdateCheckThread`）+ `ui/update_dialog.py`（`UpdateReleaseDialog`，渲染 release notes）
- **commit 版更新**（开发侧）：`ui/update_checker.py` 提供 `UpdateThread`（git fetch + reset --hard）、`CommitUpdateDialog`、`AboutDialog`
- 版本号单源：`pyproject.toml` → `utils/version.py` → `APP_VERSION`
- `scripts/generate_manifest.py` 生成 `manifest.json`（SHA256 文件清单，供 ZIP 发行增量更新）
- `utils/update_cache.py` 缓存远程提交记录以支持离线检查
- `launch.bat` 统一启动脚本

**仍待实施：**
1. CI 工作流自动化打包
2. 非 git 环境（便携版 exe）的更新支持

**参考：** `docs/基础速查/经验教训.md` §5（依赖管理改动）、`docs/基础速查/依赖库说明.md`（嵌入式 Python 环境）。
