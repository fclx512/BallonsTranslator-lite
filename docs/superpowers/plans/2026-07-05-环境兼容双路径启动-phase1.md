# 环境兼容双路径启动 Phase 1 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use subagent-driven-development or executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 `_detect_user_torch()` 从跨 Python ABI 注入改为只查当前自身，配合延迟加载确保一键包和源码运行两条路径各走各的环境，不再因版本不匹配崩溃。

**Architecture:**
- 区分 Path A（embedded Python 3.12 一键包）和 Path B（用户自己的 Python）
- `_detect_user_torch()` 简化为检查当前进程的 `torch.cuda.is_available()`
- GPU 决策段从"找不到 CUDA 就恐吓"改为"友好提示 + 自动 CPU"
- `proj_imgtrans.py` 的 `from PIL import Image` 从 module-level 移到方法内

**Tech Stack:** Python 3.10+, launch.py, launch.bat, PIL

---

### Task 1: 重写 `_detect_user_torch()` — 废除跨 Python 查找

**Files:**
- Modify: `launch.py:211-320`
- No test changes (该函数无现有测试)

- [ ] **Step 1: 确认当前函数完整范围**

当前函数位于 `launch.py:211-320`，约 110 行。功能：遍历 PATH 上所有 python.exe → 子进程探测 CUDA torch → 版本比对 → 找到则注入 site-packages。这个完整逻辑需要替换。

- [ ] **Step 2: 替换为新实现**

将 `launch.py:211-320` 内容替换为以下简洁实现：

```python
def _detect_user_torch():
    """Check if the current Python process has CUDA-capable PyTorch.

    Unlike the old implementation, this does NOT search other Pythons on
    the system.  It only checks `import torch` within the current process,
    then verifies with ``torch.cuda.is_available()``.

    Returns:
        True if CUDA-capable PyTorch is available in the current process.
        False otherwise.
    """
    try:
        import torch  # noqa: F401
    except ImportError:
        print("  PyTorch not installed in this Python environment.")
        return False

    if torch.cuda.is_available():
        print("  CUDA PyTorch available: " + str(torch.__file__))
        return True

    # torch exists but CUDA not available
    print("  PyTorch found but CUDA is not available.")
    _gpu_info = detect_gpu_info()
    if _gpu_info:
        _gen = _gpu_info["generation"]
        if _gen == "Kepler":
            print("  PyTorch 2.x may not support your Kepler GPU.")
        elif _gen == "Blackwell":
            print(
                "  Blackwell GPU requires CUDA 12.8+.\n"
                "    If using the one-click bundle, run install_cuda.bat.\n"
                "    Otherwise: pip install torch --index-url https://download.pytorch.org/whl/nightly/cu128"
            )
        else:
            print(
                f"  Recommended CUDA {_gpu_info['recommended_cuda']}"
                f" for your {_gen} GPU."
            )
    else:
        print("  Consider installing PyTorch with CUDA for GPU acceleration.")
    return False
```

- [ ] **Step 3: 验证语法正确**

Run: `python -c "import ast; ast.parse(open('launch.py').read()); print('Syntax OK')"`
Expected: `Syntax OK`

---

### Task 2: 重构 GPU 决策逻辑

**Files:**
- Modify: `launch.py:586-614`

- [ ] **Step 1: 确认当前段代码**

```python
    # GPU mode with bundled Python: detect user's system PyTorch with CUDA
    if not args.cpu and os.environ.get("BTRANSLATOR_GPU_MODE"):
        print("GPU mode: detecting user-installed PyTorch with CUDA...")
        if not _detect_user_torch():
            _gpu_info = detect_gpu_info()
            print("\n" + "=" * 60)
            print("PyTorch with CUDA was not found in your system Python.")
            ...
            print("\nSwitching to CPU mode automatically.")
            print("=" * 60 + "\n")
            args.cpu = True
            os.environ["BALLOONTRANS_CPU_ONLY"] = "1"
```

- [ ] **Step 2: 替换为新逻辑**

将 `launch.py:586-614` 替换为：

```python
    # ── GPU / CPU decision ────────────────────────────────────────────
    # Path A (one-click bundle embedded Python): BTRANSLATOR_GPU_MODE is
    #   set by launch.bat when NVIDIA GPU is detected.  We check whether
    #   this embedded Python itself has CUDA-capable torch (user ran
    #   install_cuda.bat).  If not → friendly hint + automatic CPU mode.
    #
    # Path B (user's own Python, or source run): BTRANSLATOR_GPU_MODE
    #   may or may not be set.  _detect_user_torch() checks the current
    #   process — if the user's Python has CUDA torch it works; if not,
    #   fall back to CPU.
    if not args.cpu:
        _gpu_requested = os.environ.get("BTRANSLATOR_GPU_MODE") == "1"
        if _gpu_requested:
            print("NVIDIA GPU detected — checking CUDA PyTorch availability...")

        if _gpu_requested or "ballontrans_pylibs_win" not in sys.executable:
            # Path A (GPU requested) or Path B (user Python):
            #   check if current Python has CUDA torch
            if _detect_user_torch():
                print("GPU mode: enabled")
            else:
                _is_embedded = "ballontrans_pylibs_win" in sys.executable
                if _is_embedded:
                    print("\n" + "=" * 60)
                    print("CUDA PyTorch not found in the bundled Python environment.")
                    print("To enable GPU acceleration, run: install_cuda.bat")
                    print("Or continue with CPU mode (no action needed).")
                    print("=" * 60 + "\n")
                print("Switching to CPU mode automatically.")
                args.cpu = True
                os.environ["BALLOONTRANS_CPU_ONLY"] = "1"
        # else: Path A without GPU requested → CPU mode by default
```

**关键语义变化：**
- 旧：`BTRANSLATOR_GPU_MODE` → "去找系统 CUDA torch"
- 新：`BTRANSLATOR_GPU_MODE` → "用户有 NVIDIA GPU，看看当前 Python 能不能用上"
- 嵌入式 Python 无 GPU 请求时（比如纯 CPU 机器）：不跑 `_detect_user_torch()`，跳过
- 用户自己 Python（非 embedded）：即使没设 `BTRANSLATOR_GPU_MODE` 也跑一次检测（确保 UI 显示正确的 GPU 状态）

- [ ] **Step 3: 验证语法正确**

Run: `python -c "import ast; ast.parse(open('launch.py').read()); print('Syntax OK')"`
Expected: `Syntax OK`

---

### Task 3: 延迟化 `proj_imgtrans.py` 的 PIL import

**Files:**
- Modify: `utils/proj_imgtrans.py:9`
- Modify: `utils/proj_imgtrans.py:464`
- Modify: `utils/proj_imgtrans.py:468`
- Modify: `utils/proj_imgtrans.py:502`

- [ ] **Step 1: 删除 module-level 的 PIL import**

`utils/proj_imgtrans.py:9` 删除 `from PIL import Image`

- [ ] **Step 2: 在三个使用处添加 local import**

分别在三个使用 `Image` 的地方加局部 import。

位置 1 — `proj_imgtrans.py:464`（`load_inpainted_by_imgname` 方法内）：

```python
            else:
                from PIL import Image  # noqa: F811
                i = Image.open(osp.join(self.directory, imgname))
                h, w = i.height, i.width
```

位置 2 — `proj_imgtrans.py:468`（同上方法，同一作用域已经有 import，不需要重复加）：
**注意：** 和位置 1 在同一方法中，local import 只需要一次。如果要精确保持最小改动，在第一个 `Image` 引用前加一次 import，后续引用不变。实际代码中 464 和 468 行在同个 `_load_inpainted` 方法中，所以 import 在 464 前加一次即可。

位置 3 — `proj_imgtrans.py:502`（`load_notext_by_imgname` 方法内）：

```python
        if ih != h or iw != w:
            from PIL import Image  # noqa: F811
            notext = Image.fromarray(notext).resize(
```

- [ ] **Step 3: 确认修改**

确认 `proj_imgtrans.py` 中不再有 `from PIL import Image` 以外的顶层导入。

```bash
grep -n "from PIL import\|import PIL" utils/proj_imgtrans.py
```

Expected: 无输出（已删除）

- [ ] **Step 4: 验证语法正确**

Run: `python -c "import ast; ast.parse(open('utils/proj_imgtrans.py').read()); print('Syntax OK')"`
Expected: `Syntax OK`

---

### Task 4: 新增 `install_cuda.bat`

**Files:**
- Create: `install_cuda.bat`

- [ ] **Step 1: 创建文件**

```batch
@echo off
chcp 65001 >nul
cd /d "%~dp0"
setlocal enabledelayedexpansion

:: ============================================
::  Install CUDA PyTorch into Bundled Python
::  Run this once after extracting the one-click
::  bundle to enable GPU acceleration.
:: ============================================

echo ============================================
echo  Installing CUDA PyTorch (2.7.1)
echo ============================================
echo.
echo  This will download ~2 GB of CUDA libraries.
echo  Make sure you have a stable internet connection.
echo.

:: Verify embedded Python exists
if not exist "ballontrans_pylibs_win\python.exe" (
    echo [ERROR] Embedded Python not found.
    echo  This script must be run from the one-click bundle directory
    echo  containing ballontrans_pylibs_win\.
    pause
    exit /b 1
)

echo  Detecting GPU architecture...
ballontrans_pylibs_win\python.exe -c "
import subprocess, sys
try:
    r = subprocess.run(['nvidia-smi','--query-gpu=name','--format=csv,noheader'],
        capture_output=True, text=True, timeout=10)
    if r.returncode == 0 and r.stdout.strip():
        print('  GPU: ' + r.stdout.strip())
    else:
        print('  No NVIDIA GPU detected (CUDA may still work if torch finds one)')
except Exception:
    print('  nvidia-smi not available')
"

echo.
echo  Installing torch, torchvision, torchaudio with CUDA 12.4 support...
echo.
ballontrans_pylibs_win\python.exe -m pip install ^
    torch==2.7.1 torchvision==0.22.1 torchaudio==2.7.1 ^
    --index-url https://download.pytorch.org/whl/cu124 ^
    --prefer-binary

if !ERRORLEVEL! NEQ 0 (
    echo.
    echo [ERROR] Installation failed.
    echo  Check your internet connection and try again.
    echo  If issues persist, visit https://pytorch.org for manual install instructions.
    pause
    exit /b 1
)

echo.
echo ============================================
echo  CUDA PyTorch installed successfully!
echo  Launch the app normally via launch.bat —
echo  GPU mode will be detected automatically.
echo ============================================
pause
```

---

### Task 5: 调整 `launch.bat` 注释和语义

**Files:**
- Modify: `launch.bat:163-195`

- [ ] **Step 1: 更新 GPU 检测段注释**

将 `launch.bat:163-167` 的注释从：

```batch
rem ---- 5. GPU auto-detection (skip if --cpu is set) ----
rem     Checks onnxruntime CUDA provider first, then falls back to
rem     nvidia-smi (hardware check).  If either succeeds, launch.py
rem     will try _detect_user_torch() to find the system Python's
rem     CUDA-capable PyTorch.
```

改为：

```batch
rem ---- 5. GPU auto-detection (skip if --cpu is set) ----
rem     Checks onnxruntime CUDA provider first, then falls back to
rem     nvidia-smi (hardware check).  If either succeeds, set
rem     BTRANSLATOR_GPU_MODE so launch.py knows an NVIDIA GPU is
rem     available.  launch.py will decide whether the current Python
rem     environment can actually use it (no cross-Python ABI injection).
```

其他逻辑保持不变——`BTRANSLATOR_GPU_MODE` 的检测逻辑不变，语义变了但代码一致。

---

### Task 6: 集成验证

- [ ] **Step 1: 确认没有语法错误**

```bash
python -c "import ast; ast.parse(open('launch.py').read()); ast.parse(open('utils/proj_imgtrans.py').read()); print('All files syntax OK')"
```

Expected: `All files syntax OK`

- [ ] **Step 2: 手动检查代码逻辑路径**

逐条检查以下场景的逻辑完整性：

| 场景 | BTRANSLATOR_GPU_MODE | embedded? | CUDA torch? | 期望结果 |
|------|---------------------|-----------|-------------|---------|
| 一键包 + 纯 CPU 机 | 未设置 | ✅ | ❌ | CPU 模式，_detect_user_torch 不跑 |
| 一键包 + 有 GPU + 未装 CUDA | 1 | ✅ | ❌ | 打印 install_cuda 提示 → 自动 CPU |
| 一键包 + 已装 CUDA | 1 | ✅ | ✅ | GPU 模式 |
| 源码 run + py launcher + 有 CUDA | 1 | ❌ | ✅ | GPU 模式（同一 Python 无 ABI 问题） |
| 源码 `python launch.py` + 有 CUDA | 未设置 | ❌ | ✅ | Path B 分支跑 _detect_user_torch → GPU |

- [ ] **Step 3: 确认 `install_cuda.bat` 可执行**

```bash
python -c "
# 验证 bat 中引用的 Python 路径和 pip 命令
# 实际运行需要 Windows 环境
print('install_cuda.bat created: OK')
print('Target: ballontrans_pylibs_win\\python.exe -m pip install torch...')
"
```

- [ ] **Step 4: 提交**

```bash
git add -A
git diff --cached --stat
```

确认以下文件被改动：
- `launch.py` — `_detect_user_torch()` 重写 + GPU 决策段重构
- `utils/proj_imgtrans.py` — PIL import 延迟化
- `launch.bat` — 注释更新
- `install_cuda.bat` — 新文件（未跟踪 → 自动被 git add -A 暂存）

然后提交：

```bash
git commit -m "refactor: 废除跨 Python ABI 注入，实施双路径启动

- _detect_user_torch() 从遍历系统 Python 查找 CUDA torch 改为
  仅检查当前进程自身，根除 ABI 版本不匹配问题
- GPU 决策段区分一键包(Path A)和源码运行(Path B)两条路径
- proj_imgtrans.py 的 PIL import 延迟化，确保缺 PIL 也能进 UI
- 新增 install_cuda.bat，一键包用户可选安装 CUDA torch
- launch.bat 注释同步更新"
```
