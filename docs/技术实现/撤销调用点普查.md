# 撤销体系调用点普查清单

> **3b 实机验收通过（2026-09-05，用户确认满足预期）——本清单使命完成，留作普查方法论与改造边界的存档。**
>
> 阶段 0-a 产物（2026-09-02）。全库普查 `.undo()` / `.redo()` / `availableUndoSteps` / `joinPreviousEditBlock` / `setUndoRedoEnabled` / `old_undo_steps` 的全部调用点，逐个标注终局（单栈快照命令制）下的去向。本清单是阶段 3 的改造边界：`3b` 禁用文档栈前，B/C/D/E 四类必须全部落地完毕。`modules/`、`utils/`、`launch.py` 无任何命中。
>
> **3b 已落地（2026-09-05）**：C/D/E 三类已全部删除；item/panel 文档在构造时即 `setUndoRedoEnabled(False)`（`ui/text_engine/item.py::TextBlkItem` 与 `ui/textedit_area.py::SourceTextEdit` 的 `__init__`，后者覆盖 `TransTextEdit` 子类）；信号签名随之瘦身（`push_undo_stack` 去步数参数、`propagate_user_edited` 去 `joint_previous` 参数）。B 类已在 3a 快照化。普查新发现（超出原计划预期）：
>
> 1. **`ui/drawing_commands.py::RunBlkTransCommand` 跨界依赖**：绘制栈命令的文本回退直接调 `blkitem`/`e_trans`/`e_source` 的文档级 undo/redo（B-5）。
> 2. **页面内查找替换同样依赖文档栈**：`ui/textedit_commands.py::PageReplaceOneCommand` / `ui/textedit_commands.py::PageReplaceAllCommand`（B-3/B-4）以文档 undo/redo 做重放——它们属于 PageSearchWidget 页面内替换，**不是**计划「分治不动」的 GlobalReplaceApplier 快照回滚体系，必须纳入 3a 快照化。

## A. 命令栈入口（QUndoStack 层）——终局保留，不动

| 位置 | 说明 |
| --- | --- |
| `ui/canvas.py::undo_textedit` / `ui/canvas.py::redo_textedit` / `ui/canvas.py::undo` / `ui/canvas.py::redo` | text_undo_stack 的撤销重做入口，终局唯一栈 |
| `ui/context_menu_config.py`（右键命令）、`ui/mainwindow.py`（菜单/快捷键） | 仅调 `canvas.undo()/redo()`，不经文档栈 |

## B. 命令类内部对文本文档的 undo/redo —— 3a 改快照重放

| 位置 | 调用（改造前） | 说明 |
| --- | --- | --- |
| `ui/textedit_commands.py` 的 `TextItemEditCommand`（3a 已删除） | 曾调 `blkitem.redo()/undo()`、`edit.redo()/undo()` | 画布键入与格式化命令；3a 起由 TypingSessionCommand/FormatGestureCommand 快照命令取代 |
| `ui/textedit_commands.py` 的 `TextEditCommand`（3a 已删除） | 曾调面板编辑器文档排水 | 面板键入命令；同上被快照命令取代 |
| `ui/textedit_commands.py::PageReplaceOneCommand` | 曾调 `blkitem.undo()/redo()`、`edit.undo()/redo()` | **页面内替换单个**（PageSearchWidget 体系，非 GlobalReplaceApplier）；3a 已改快照重放 |
| `ui/textedit_commands.py::PageReplaceAllCommand` | 同上，批量 | **页面内替换全部**；3a 已改快照重放 |
| `ui/drawing_commands.py::RunBlkTransCommand` | 曾调 `blkitem.redo()/undo()`、`transpairw.e_trans.redo()/undo()`、`e_source.redo()/undo()` | **跨界依赖**：绘制栈上的管线运行命令，文本回退曾借道文档私有栈；3a 已改快照重放 |
| `ui/textedit_commands.py::MultiPasteCommand` | 曾调 `blkitem.undo()/redo()`、`etran.undo()/redo()` | 多块粘贴命令；3a 已改快照重放 |

## C. 文档级 undo/redo 封装（排水口）—— 3b 已删除

| 位置 | 说明 |
| --- | --- |
| `ui/text_engine/item.py::TextBlkItem` 的文档排水口 | 已删除（3b）：文档级 undo/redo 方法随步数记账一并移除，撤销唯一入口是 `ui/canvas.py::undo_textedit` |
| `ui/textedit_area.py` 面板编辑器的文档级 undo/redo | 已删除（3b）：同上；面板 Ctrl+Z 走 `undo_signal` → `ui/scenetext_manager.py::on_textedit_undo` → canvas |

## D. 步数记账 —— 3b 已删除

| 位置 | 说明 |
| --- | --- |
| item 侧 `old_undo_steps` 全部读写点 | 已删除（3b） |
| `ui/textedit_area.py` 面板侧 `old_undo_steps` 全部读写点 | 已删除（3b） |
| `ui/textedit_commands.py::sync_text_by_diff` 内的水位回写 | 已删除（3b） |
| `_refresh_undo_steps`（批量替换辅助，已删除） | 已删除（3b）：随 `updateUndoSteps` 方法一并移除；`_suppress_change_sync` 的 `in_redo_undo` 抑制旗保留（重放守卫仍在用） |

## E. 块合并语义（joint 链）—— 3b 已删除

| 位置 | 说明 |
| --- | --- |
| `ui/textedit_commands.py::sync_text_by_diff` 的 `joinPreviousEditBlock` 分支 | 已删除（3b）：`joint_previous` 参数链整体移除，对账恒为独立编辑块；`propagate_user_edited`/`push_undo_stack` 信号签名随之瘦身 |

## F. setUndoRedoEnabled —— 逐处核实结论

| 位置 | 结论 |
| --- | --- |
| `ui/text_engine/annotations.py` 富文本 HTML 装载处 | **安全**：保存 `isUndoRedoEnabled()` 旧值、finally 恢复旧值，全局禁用后恢复的是 False |
| `ui/text_engine/annotations.py` 剪贴板 MIME 构造处 | 独立暂存 QTextDocument，不动 |
| `ui/text_engine/effects/renderer.py` 描边克隆文档 | 独立暂存文档，不动 |
| `ui/text_engine/rendering/native_document.py` 字形测量文档 | 独立暂存文档，不动 |
| `ui/global_search_widget.py` 两处 | GlobalReplaceApplier 快照暂存文档，不动（分治体系） |

## G. 休眠拷贝 —— 不动，仅登记

`ui/text_engine/editing/upstream_commands.py`、`ui/text_engine/editing/widgets.py` 全部命中均为休眠拷贝内的同符号，不作为参考、不做改造。

## H. 观察项

| 位置 | 说明 |
| --- | --- |
| `ui/textedit_area.py::setPlainTextAndKeepUndoStack` / `ui/text_engine/item.py::setPlainTextAndKeepUndoStack` | 3b 核实完毕：文档栈禁用后「保留文档历史」语义已自然消解，现有调用方（绘制栈命令重放、GlobalReplaceApplier、页面内替换重放）只依赖其「全文替换保留区外字符格式」效果，名字保留未改 |
| `ui/drawing_commands.py::EmptyCommand` | 空命令占位，不经文档栈，不动 |
