# 设置面板概述

> 最后更新：2026-08-14（大修：分页结构刷新为 12 页、行号引用改为 `路径::符号`、删除已失效符号引用）

设置面板（`ConfigPanel`）在 2026-06-23 重构后改为 **内部分页 + 中心淡入模态**，不再使用右侧滑入长卷轴；2026-08-13 又做了一次**两列表单排版重构**（`ConfigFormRow`，见 `docs/基础速查/设置面板排版思路.md`）。以下是与设置面板相关的全部文件、符号、分页结构、动画机制、备注系统、子对话框屏蔽以及 i18n 注意事项，供后续 AI 接手时快速了解全貌。

---

## 相关文件

| 文件 | 用途 |
|------|------|
| `ui/configpanel.py` | ConfigPanel 主体、ConfigTable 导航树、分页、信号、保存、ConfigSubBlock/ConfigFormRow、ConfigNotePopup |
| `ui/overlay_modal.py` | OverlayModal（scrim + 淡入淡出承载） |
| `ui/mainwindow.py` | MainWindow 接入点：创建 OverlayModal、注入 `_modal_ref`、`to_*_config()` |
| `utils/config.py` | `pcfg` 全局配置单例、`save_config()` |
| `utils/shared.py` | NAVLIST_WIDTH、CONFIGBLOCK_CONTENT_MARGINS 等常量 |
| `ui/custom_widget/combobox.py` | `ConfigComboBox` 的 `scrollWidget` 参数 |
| `config/stylesheet.css` | `QPushButton#ConfigButton` 规则 |
| `ui/misc.py` | `get_theme_color()` — 备注 ? 按钮取色 |

---

## 分页结构

12 个页面，`QStackedWidget`（`ConfigPanel.pageStack`，`ui/configpanel.py::pageStack`）。页面内容包裹在 `QScrollArea` + `ConfigScrollBar` 中（`_wrap_page`，`ui/configpanel.py::_wrap_page`）。

导航使用 `ConfigTable`（树形控件，`ui/configpanel.py::ConfigTable`），不再使用上游的 `NavList`。

```
导航树（2 个 header，12 条目）            PageStack 索引
────────────────────────────────────────────────
Modules（header）
  Module Actions（Models 组）                 0
  Text Detection                              1
  OCR                                         2
  Inpaint                                     3
  Translator                                  4
  LLM Profile                                 5
General（header）
  Project                                     6
  Typesetting                                 7
  Interface                                   8
  Shortcuts                                   9
  Quick Menus                                10
  App（配置导入导出 + Updates）               11
```

> 说明：2026-08-13 之后管线被拆成 4 个独立页面（Detect/OCR/Inpaint/Translator），LLM 配置改为内联 `ProfileManagerWidget` 页面（`utils/profile_manager.py::ProfileManagerWidget`），新增 Shortcuts、Quick Menus、App 三个页面；原「Pipeline」「Performance」两页已不存在。

### 页面创建机制

`ConfigPanel` 的构建分成两条路径：

- **`_add_page(content)`**（`ui/configpanel.py::_add_page`）：把裸 widget（如 Models 组、LLM Profile 页）直接注册为独立页面。
- **`_add_grouped_page(...)`**（`ui/configpanel.py::_add_grouped_page`）：构建 `PanelGroupBox` + `ConfigSubBlock`，把 `section_widget` 注册为页面（用于 Detect/OCR/Inpaint/Translator）。
- **`addGroupedBlock(...)`**（`ui/configpanel.py::addConfigBlock` 的 legacy shim → `_DeadBlock`）：General 组的 Project/Typesetting/Interface/App 通过它注册，内部仍路由到 `pageStack`。

`_wrap_page(content: QWidget) -> QScrollArea`（`ui/configpanel.py::_wrap_page`）将每个 section 包裹为带 `CONFIGBLOCK_CONTENT_MARGINS` 的滚动页面。内容 widget 的垂直策略设为 `Fixed` 防止短页面被拉满窗口。

页面注册顺序（决定 `pageStack` 索引）：

```text
0  models_group             _add_page
1  detect_config_panel      _add_grouped_page
2  ocr_config_panel         _add_grouped_page
3  inpaint_config_panel     _add_grouped_page
4  trans_config_panel       _add_grouped_page
5  llm_profiles_panel       _add_page
6  project_block            addGroupedBlock
7  typesetting_block        addGroupedBlock
8  interface_block          addGroupedBlock
9  shortcuts_editor         _add_page
10 quick_menus_editor       _add_page
11 config_mgmt_block        addGroupedBlock
```

### ConfigSubBlock 与 ConfigFormRow 容器

- **`ConfigSubBlock`**（`ui/configpanel.py::ConfigSubBlock`）：老式「标题 + 控件」容器，仍用于 `_add_grouped_page` 里包裹模块选择面板。
- **`ConfigFormRow`**（`ui/configpanel.py::ConfigFormRow`，继承 ConfigSubBlock）：2026-08-13 引入的**两列表单**容器（左列固定宽度右对齐 label + 右列控件 + 可选 `?` 帮助按钮）。布局约定详见 `docs/基础速查/设置面板排版思路.md`。

### ConfigNotePopup 备注浮层

```python
class ConfigNotePopup(QFrame):                  # ui/configpanel.py::ConfigNotePopup
    """锚定到 ? 按钮，Qt.Popup 自动失焦关闭，展开动画受 pcfg.animation_fps 控制"""
```

- 由 `_make_note_btn(note_text)`（`ui/configpanel.py::_make_note_btn`）生成 `?` 按钮，点击弹出。
- 默认锚定到 `?` 按钮左侧 12px，垂直居中；左侧不足时翻转到右侧。
- 内容 QLabel，wordWrap，maxWidth 320px。
- 展开动画 OutCubic 200ms；`pcfg.animation_fps < 0` 时直接出现。

---

## 导航树分页连线

导航使用 `ConfigTable`（树形 `QTreeView` 子类，`ui/configpanel.py::ConfigTable`）：

- `configTable.section_pressed`（信号，`ui/configpanel.py::section_pressed`）→ `_on_nav_section_pressed(key)`（`ui/configpanel.py::_on_nav_section_pressed`）：读取 `_nav_section_to_widget[key]`（`ui/configpanel.py::_nav_section_to_widget`）取出对应 section_widget，通过 `pageStack` 的 section→index 映射切换。
- `focusOnDetect/OCR/Inpaint/Translator`（`ui/configpanel.py::focusOnDetect` 等）：先切到对应管线页，再滚动定位。
- `focusOnLLMProfile(profile_id)`（`ui/configpanel.py::focusOnLLMProfile`）：切到 LLM Profile 页。
- Models 页默认选中。

`_nav_section_to_widget` 的 key：`models` / `detect` / `ocr` / `inpaint` / `trans` / `llm_profile` / `project` / `typesetting` / `interface` / `shortcuts` / `quick_menus` / `config_mgmt`。

---

## OverlayModal 机制

### 载体

`ui/overlay_modal.py` 的 `OverlayModal(QObject)`（`ui/overlay_modal.py::OverlayModal`）。它在 `cover_widget`（`centralStackWidget`）上方创建一个 `_Scrim(QWidget)`（`ui/overlay_modal.py::_Scrim`），面板（ConfigPanel）位于 scrim 之上居中。

### 核心参数

```python
_PANEL_W = 1000      # 面板固定宽（cover 小则缩）
_PANEL_H = 700       # 面板固定高
_SCRIM_ALPHA = 0.55  # 遮罩不透明度目标值
_DEFAULT_DURATION = 350  # 动画时长 ms
```

### 动画

scrim 和 panel 各有 `QGraphicsOpacityEffect`，用 QTimer 按 `_detect_interval()`（= `pcfg.animation_fps` 换算的帧间隔）驱动，`QEasingCurve.InOutExpo` 插值。

`pcfg.animation_fps < 0` 或 `duration ≤ 0` 时跳过动画。动画过程中收到反向操作会平滑反转（`_reverse`）。

### 关闭路径

- **scrim 点击**：`_Scrim.mousePressEvent` 检查 `_closable`，为 True 时调用 `OverlayModal.hide()`（通过 `on_click_outside` 回调）。子对话框打开期间 `_closable=False`。
- **配置图标再次点击**：`leftBar.configChecker` 互斥 → `setupConfigUI()` → 已在显示时 `_hideConfigOverlay()`。
- **imgTrans 互斥切换**：`leftBar.imgTransChecker` 被点 → `_hideConfigOverlay()`。
- **Esc 关闭**：ConfigPanel 内 `QShortcut(Qt.Key_Escape)` → `_close_via_esc`。
- **outside-click 自动隐藏**：ConfigPanel 通过全局 eventFilter（`_installOutsideClickFilter` / `_removeOutsideClickFilter`）拦截点击面板外的鼠标事件。

### scrim 范围

scrim 只覆盖 `cover_widget`（`centralStackWidget`，即 canvas 区域 + 右侧翻译面板）。**左栏 leftBar、底栏 bottomBar、标题栏 titleBar 在 scrim 外部**，仍可点击。

### MainWindow 接入点

| 代码 | 位置 |
|------|------|
| `from .overlay_modal import OverlayModal` | `ui/mainwindow.py` |
| OverlayModal 创建、`_modal_ref` 注入 | `ui/mainwindow.py`（`self.configPanel._modal_ref = self._configModal`） |
| `_showConfigOverlay` | `ui/mainwindow.py::_showConfigOverlay` |
| `_hideConfigOverlay` | `ui/mainwindow.py::_hideConfigOverlay` |
| `_on_config_hidden` | `ui/mainwindow.py::_on_config_hidden` |
| `to_trans_config` / `to_inpaint_config` / `to_ocr_config` / `to_detect_config` | `ui/mainwindow.py::to_trans_config` 等 |

---

## 子对话框 backdrop 屏蔽

模态子对话框调用统一经 `_run_modal_dialog(dialog)`（`ui/configpanel.py::_run_modal_dialog`）：

```python
def _run_modal_dialog(self, dialog) -> int:
    if self._modal_ref is not None:
        self._modal_ref.set_backdrop_closable(False)
    result = dialog.exec()
    if self._modal_ref is not None:
        self._modal_ref.set_backdrop_closable(True)
    return result
```

涉及对话框：`on_exclude_fonts_clicked`（`ui/configpanel.py::on_exclude_fonts_clicked`）、`_open_network_settings`（`ui/configpanel.py::_open_network_settings`）。

注：`_open_shortcut_dialog` 已于 Shortcuts 页面化后删除；`_open_profile_manager` 已于 LLM 配置内联后删除。

---

## 信号与保存

| 信号 | 用途 | 连接方向 |
|------|------|---------|
| `save_config` | 触发全局配置落盘 | ConfigPanel → MainWindow |
| `reload_textstyle` | 强制重载文字样式 | ConfigPanel → MainWindow |
| `font_exclusion_changed` | 字体排除表变更 | ConfigPanel → MainWindow |
| `shortcuts_changed` | 快捷键配置变更 | ConfigPanel → MainWindow |
| `presets_changed` | 预设值变更 | ConfigPanel → MainWindow |
| `unload_models` | 卸载全部模型 | ConfigPanel 内部 |
| `profiles_changed` | API 配置变更 | ConfigPanel 内部 |
| `check_update` / `check_commit_update` | 检查更新请求 | ConfigPanel → MainWindow |

**保存时机**：`ConfigPanel.hideEvent`（`ui/configpanel.py::hideEvent`）发射 `save_config`。没有"应用/取消"按钮，关闭时一次性保存。`closeEvent`（`ui/configpanel.py::closeEvent`）委托给 modal hide。

---

## i18n 注意事项

1. 所有 UI 文字都用 `self.tr("English")`。
2. ts 中 `<context>` 对应类名。
3. `_ShortcutRow`（`ui/configpanel.py::_ShortcutRow`）使用 `self.tr(_ACTION_NAMES[id])` 间接调用，i18n_check 始终报 orphan（假阳性），需在 ts 对应 context 手工维护。
4. **QM 编码陷阱**：qm_compile 的 `_iso8859_str()` 须用 utf-8（已修复），否则 `—/→/✓` 被替成 `?` 导致 Qt 哈希查找失败。
5. 编译验证：
   ```bash
   python scripts/qm_compile.py translate/zh_CN.ts translate/zh_CN.qm
   python scripts/i18n_check.py          # 日常
   python scripts/i18n_check.py --ci     # 发版（对 orphan 退出码 4）
   ```

---

## 统一样式规则

以下规则适用于所有设置面板内的控件：

| 规则 | 实现方式 |
|------|---------|
| **按钮宽度** | `QPushButton#ConfigButton`，所有设置页按钮均设此 objectName |
| **下拉栏滚轮屏蔽** | 所有 `ConfigComboBox` 指定 `scrollWidget=self`，未聚焦时滚轮转发至页面滚动 |
| **SpinBox 去箭头** | 所有 `QSpinBox` 设 `setButtonSymbols(QAbstractSpinBox.NoButtons)` |
| **统一容器** | 表单行用 `ConfigFormRow`，模块选择页用 `ConfigSubBlock`，章节头用 `_section_header`（`ui/configpanel.py::_section_header`） |

## 已知瑕疵 / 待现场微调

1. **Panel 固定 1000×700**：字体委派网格等宽控件的布局在窄裁切窗口下可能溢出。已在代码中预留 `fit window` 判据（cover 小于 1000 则缩）。
2. **导航树层级**：Models 的 PanelGroupBox 顶边与导航树顶部无对齐约束，由左右独立布局控制。
3. **`_is_canvas_mode`**：读取 `panel.isVisible()` 抑制画布快捷键（`ui/mainwindow.py::_is_canvas_mode`）。

---

## 测试流程（供后续 AI 参考）

首选统一入口 `./ballontrans_pylibs_win/python.exe scripts/verify.py`（语法 → 文档 → i18n → qm → 冒烟）。改动命中启动链文件 `ui/configpanel.py` 时自动触发启动冒烟测试 `tests/test_startup_imports.py`（含 `ProfileManagerWidget` 实例化）。
