# 反向移植工作进度交接（2026-08-17 更新：四项实机验证通过，功能1 UI 待适配，PR 未推送）

> 会话交接文档：把 fork（BallonsTranslator-lite）的 4 个功能反向移植到上游 dmMaze/BallonsTranslator 的工作进度，供下一个模型接手。
> 完整规范见 `docs/技术实现/反向移植_规范.md`，技能见 `.agents/skills/backport-upstream/`。

## 一、任务总览

用户已拍板的 4 个功能（按依赖顺序，一次一功能一 PR）：

| # | 功能 | 状态 | 分支 | worktree 目录 |
|---|------|------|------|--------------|
| 1 | 设置面板快捷键页面（基础，后续功能靠它绑快捷键） | ✅ 完成并提交（⚠️ UI 风格适配待处理，见下） | `feat/backport-shortcuts` | `D:\ruanjian\BallonsTranslator-backports` |
| 2 | 序号徽标（重排的视觉反馈） | ✅ 完成并提交 | `feat/backport-seq-badge` | `D:\ruanjian\BallonsTranslator-badge` |
| 3 | 路径重排（画路径定义阅读顺序） | ✅ 完成并提交（2026-08-17，含 merge 冲突修复） | `feat/backport-path-reorder` | `D:\ruanjian\BallonsTranslator-reorder` |
| 4 | 点击空白弃选（完全弃选） | ✅ 完成并提交（2026-08-17） | `feat/backport-click-deselect` | `D:\ruanjian\BallonsTranslator-deselect` |

**四个分支均已本地提交、未推送 myfork、未开 PR。** 推送/开 PR 由用户操作（见第八节）。

**上游基线**：`upstream/dev` = `81f1e79`（v1.5.11）。⚠️ 上游 dev 已改用 `ballontranslator/ui/text_engine/` 结构（`item.py`/`layout.py`/`formatting` 等），旧 `textitem.py`/`text_panel.py`/`scene_textlayout.py` 已删除——徽标/重排的落点是 `ballontranslator/ui/text_engine/item.py`。

## 二、仓库与分支状态

主克隆 `D:\ruanjian\BallonsTranslator` 停在 `userspace_update` 分支（旧基线 `391c8eb` + 大量未提交改动）。**绝不要动它**——那是用户的个人同步区。移植工作全部在 worktree 里做：

```
D:\ruanjian\BallonsTranslator-backports   feat/backport-shortcuts      （功能1，2 提交）
D:\ruanjian\BallonsTranslator-badge       feat/backport-seq-badge      （功能2，2 提交）
D:\ruanjian\BallonsTranslator-reorder     feat/backport-path-reorder   （功能3，merge+2 提交）
D:\ruanjian\BallonsTranslator-deselect    feat/backport-click-deselect （功能4，1 提交，基于 dev）
```

分支关系：
- `feat/backport-shortcuts`、`feat/backport-seq-badge` 都基于 `upstream/dev`（81f1e79）。
- `feat/backport-path-reorder` = 基于 badge 分支 + merge shortcuts（merge 提交 `474a459`，原 `0552a17` 含 configpanel 冲突标记，2026-08-17 已修复并用 `--amend` 重写；额外 2 提交：feat `9f53eab` + i18n `7e79244`）。
- `feat/backport-click-deselect` 独立基于 dev，提交 `853d624`（仅 canvas.py 13 行，无 i18n 字符串）。
- ⚠️ **hash 对齐待办**：shortcuts 分支 i18n 提交已被 amend（`3d00008`→`ec47709`），reorder 分支 merge 的仍是旧 `3d00008`。**推 PR 前把 reorder rebase 到最新 `feat/backport-shortcuts`**，否则 shortcuts PR 合并后 reorder 的 diff 会出重复/冲突补丁。

## 三、已完成功能详情

### 功能 1：快捷键页面（feat/backport-shortcuts）

提交：`f1d0c63`（代码）+ `ec47709`（i18n，含 pt_BR 修复的 amend）。

改动文件：
- **新增** `ballontranslator/utils/shortcut_conflicts.py` — `find_conflict_keys(mapping)` 纯函数。
- **新增** `ballontranslator/ui/shortcut_editor.py` — `DEFAULT_SHORTCUTS`（26 个动作，默认键=上游原硬编码值）、`_ACTION_NAMES`、`_SHORTCUT_GROUPS`（6 组）、`resolve_shortcut_keys(action_id)`（pcfg 覆盖回退默认）、`_ShortcutRow`（行：动作名 + 键位药丸 + /+/Del/Rst 按钮，QKeySequenceEdit 录制，冲突红显）、`ShortcutEditor`（PanelGroupBox 分组，`shortcut_changed` 信号）。
- `ballontranslator/utils/config.py` — `ProgramConfig` 加 `shortcuts: Dict[str, List[str]] = field(default_factory=dict)`（旧 JSON 兼容，空则回退默认）。
- `ballontranslator/ui/configpanel.py` — General 组下加 "Shortcuts" 章节（`addConfigBlock` 模式）；`shortcuts_changed` 信号；`_on_shortcuts_changed` 转发 `save_config` + `shortcuts_changed`。
- `ballontranslator/ui/mainwindow.py` — `setupShortcuts()` 重写为注册表式：`shortcut_registry` + `_install_shortcuts()` + `refreshShortcuts()`（实时重建）+ `_make_shortcuts()`；drawpanel H/R/J/B 也走注册表（保留 `shortcutSetCurrentToolByName` 的 key 参数，多键序列传 None）；`configPanel.shortcuts_changed → refreshShortcuts`。
- `ballontranslator/ui/mainwindowbars.py` — 移除 titleBar QAction 硬编码快捷键：undo(Ctrl+Z)、redo(Ctrl+Y)、page_search(Ctrl+F)、global_search(Ctrl+G)、drawBoard(P)、textedit(T)、merge_tool(Ctrl+Shift+M)。**保留** Open(Ctrl+O)/Save(Ctrl+S)/F5。
- `resources/stylesheet.css` — 追加 `ShortcutActionName`/`ShortcutPill`/`ShortcutPillLabel`/`ShortcutAddBtn`/`ShortcutDelBtn`/`ShortcutRstBtn`/`ShortcutPillCloseBtn`/`ShortcutNoneLabel` 样式（用 `@` 主题变量 + rgba，`[conflict="true"]` 动态属性选择器）。
- 9 个 `.ts`：`_ShortcutRow` context 30 条（动作名+占位符+提示），`ShortcutEditor` context 6 条（组名），`ConfigPanel` context 1 条（"Shortcuts"）；zh_CN 全译，其余 unfinished。

**⚠️ 已知问题（2026-08-17 用户实机验证反馈）：快捷键页面 UI 显示不适配上游风格**。快捷键页的控件/样式（`ShortcutPill`/`ShortcutAddBtn` 等 QSS + `ShortcutActionName` 布局）是 fork 自带风格，放到上游 settings 面板里出现显示问题（对不齐/风格突兀）。**用户不擅长 UI 且上游作者有自己的一套风格（上游 AGENTS.md UI Styling Rules）→ 此项标注为"需处理"，不作为阻塞项，但推 PR 前必须：① 在 PR 描述中注明"快捷键页 UI 风格待作者按上游规范调整"；② 或由下一轮适配上游 `PanelGroupBox`/frameless 规范重做该页样式。功能逻辑已实机验证无 bug（生效、冲突检测、持久化均正常），仅外观待处理。**

### 功能 2：序号徽标（feat/backport-seq-badge）

提交：`e6635c2`（代码）+ `955474b`（i18n）。

改动文件：
- `ballontranslator/ui/text_engine/item.py` — `TextBlkItem.paint()` 末尾调 `_paint_seq_badge()`：左上角圆角黑底白字徽标，显示 `idx + 1`（阅读顺序）；选中态用主题色底；字体 `11/scale`（`topLevelItem().scale()` 补偿缩放，fork 同款思路）；**挂 `_ui_guide_suppressed`（导出时隐藏，等效 fork 的 `_hide_badge`）和 `pcfg.show_seq_badge`**。
- `ballontranslator/utils/config.py` — `show_seq_badge: bool = True`。
- `ballontranslator/ui/configpanel.py` — Typesetting 章节加 "Sequence Badge" 复选框；`show_seq_badge_changed` 信号；handler 写 pcfg + 发信号 + save_config。
- `ballontranslator/ui/mainwindow.py` — `configPanel.show_seq_badge_changed → canvas.update`。
- 9 个 `.ts` 加 "Sequence Badge"（ConfigPanel context）。

**已验证**：徽标开/关/导出抑制三态像素计数测试（开关阈值 150 区分徽标与文字字形）、MainWindow 实例化、复选框联动（注意：`setupConfig()` 由 MainWindow 调，直接实例化 ConfigPanel 不会初始化勾选态）。

## 四、功能 3：路径重排（✅ 已完成 2026-08-17）

**已实现并提交**（feat `9f53eab` + i18n `7e79244`，叠加于 merge `474a459` 之上）。以下为移植源与适配记录（供后续维护/PR 描述参考）。

### fork 参考实现（移植源）

- **canvas 侧** `fork:ui/canvas.py`：
  - 状态字段（`__init__` 里）：`_reorder_mode`/`_reorder_drawing`/`_reorder_path`(QPainterPath)/`_reorder_path_item`(QGraphicsPathItem)/`_reorder_touched_blocks`(List[TextBlock])/`_reorder_brush_radius=20.0`；信号 `reorder_path_finished = Signal(list)`（line 304）。
  - `enterReorderMode()`(1047)：置模式、十字光标、禁拖拽、建虚线路径 item（`QPen(QColor(52,152,219,150),4)` DashLine，zValue 150，parent=textLayer）。
  - `exitReorderMode()`(1070)：清理、复位光标/拖拽、重置所有 TextBlkItem 的 `_reorder_seq=-1`。
  - `_reorder_start_stroke(scene_pos)`(1092)：`_reorder_path.moveTo(textLayer.mapFromScene(scene_pos))`（坐标系转 textLayer 局部）。
  - `_reorder_extend_stroke(scene_pos)`(1100)：lineTo 延伸；`QPainterPathStroker`（width=`_reorder_brush_radius*2/scale_factor`，随 zoom 反比保持手感）做碰撞；未触碰块若 `stroked.intersects(item.absBoundingRect(qrect=True))` → 记入 touched 列表 + setSelected + `_reorder_seq=len-1` + update（这就是重排时的实时序号徽标）。
  - `_reorder_end_stroke()`(1132)：发 `reorder_path_finished(touched_ids)`（touched_ids 是 canvas 全部块索引，含未保存新块）。
  - **mousePressEvent 钩子**：`if self._reorder_mode and btn==LeftButton: _reorder_start_stroke(...); return`（在鼠标按键分支最前面）。
  - **mouseReleaseEvent 钩子**：`if self._reorder_drawing and btn==LeftButton: _reorder_end_stroke(); return`。
- **徽标覆盖** `fork:ui/textitem.py`：`__init__` 里 `_reorder_seq = -1`；`_draw_seq_badge` 里 `if self._reorder_seq >= 0: seq_text = str(self._reorder_seq+1) else: str(self.idx+1)`。上游对应改 `ballontranslator/ui/text_engine/item.py` 里的 `_paint_seq_badge`。
- **mainwindow 侧** `fork:ui/mainwindow.py`：
  - `on_path_reorder()`(2399)：已在重排模式则忽略；项目空/无当前图/无文字块给 warning；`reorder_path_finished.connect(_on_reorder_path_done)` + `enterReorderMode()`。
  - `_on_reorder_path_done(touched_ids)`(2428)：disconnect；touched<2 → "Continue Drawing/Cancel" 对话框；否则 "Apply/Continue Drawing/Cancel" 三选一；Apply → `imgtrans_proj.pages[current_img] = reordered + untouched`（reordered 按 touched 顺序）→ `canvas.updateCanvas()` → `st_manager.updateSceneTextitems()` → `exitReorderMode()`。**英文文案**，所有 `self.tr()`。
  - 快捷键注册：fork 在 `_install_shortcuts` 里 `path_reorder → on_path_reorder`（默认无键）。
- **入口**：fork 是 titleBar 按钮 `smart_reorder_trigger` + 快捷键。用户已拍板上游用**工具栏按钮 + 快捷键**（`path_reorder` 动作默认无键）。

### 上游侧适配点（已确认）

- 上游 `canvas.py` 有 `textLayer`（QGraphicsPixmapItem，`mapFromScene` 可用）、`gv`（视图，`unsetCursor`/`restore_drag_mode` 需确认存在，fork 的 `restore_drag_mode` 上游可能叫别的）、`scale_factor`、`updateCanvas()`、`absBoundingRect(qrect=True)` API 相同。
- 上游 `proj_imgtrans.pages` 结构与 fork 相同（`Dict[str, List[TextBlock]]`），重排逻辑直接照搬。
- 上游 SceneTextManager 刷新方法名需确认（fork 用 `updateSceneTextitems`）。
- 重排模式需与上游 grid/projective transform 控制互斥（fork 重排时禁拖拽）。
- 快捷键注册复用功能 1 的 `_install_shortcuts`（在 reorder 分支里直接加 `self.shortcut_registry["path_reorder"] = self._make_shortcuts("path_reorder", self.on_path_reorder)`，`DEFAULT_SHORTCUTS` 加 `"path_reorder": []`）。
- 对话框确认按钮文案英文 + `self.tr()`；`.ts` 加消息（zh_CN 译：路径重排/应用/继续绘制/取消等，可参考 fork `translate/zh_CN.ts` 的 `_ShortcutRow` context 里 "Path Reorder"→路径重排）。

## 五、功能 4：点击空白弃选（✅ 已完成 2026-08-17）

**已实现并提交**（`853d624`，独立分支基于 dev，仅 `canvas.py` 13 行，无 i18n 字符串）。

- fork 参考：`fork:ui/canvas.py::mousePressEvent` 末尾（1252-1263）：
  ```python
  if btn == Qt.MouseButton.LeftButton and self.txtblkShapeControl.isVisible():
      items_at = self.items(event.scenePos())
      if not any(isinstance(item, TextBlkItem) or item.data(CONTROL_ITEM_DATA_KEY) for item in items_at):
          self.txtblkShapeControl.setBlkItem(None)
          self.clear_text_transform_controls()
  ```
- **已实测**：Qt 默认对空白点击本就会 clearSelection（baseLayer 不拦截），上游残留的只是形状控制框（`on_selection_changed` 只 `endEdit()` 不隐藏框）。所以只需移植 fork 这段隐藏框逻辑即可达到**完全弃选**（用户已拍板语义）。
- 上游版插入位置：`ballontranslator/ui/canvas.py` 的 `mousePressEvent` 尾部 `return super().mousePressEvent(event)` 之前；`clear_text_transform_controls`（~389）、`txtblkShapeControl.setBlkItem(None)`、`CONTROL_ITEM_DATA_KEY` 均已存在。
- **验证**：因 PyQt6 捆绑版无法构造 `QGraphicsSceneMouseEvent` 且 QTest 合成事件会原生崩溃，采用"运行时从源码提取真实 `mousePressEvent` 函数体、仅把结尾 `super()` 场景派发替换为 no-op、以假事件对象驱动"的逻辑级测试——空白点击隐藏并解绑形状控制 ✓、点击块本身保留 ✓、点击覆盖物保留 ✓。

## 六、验证环境与方法（重要）

- 上游克隆**没有 python 环境**。用 fork 捆绑 python：`D:\ruanjian\BallonsTranslator-lite\ballontrans_pylibs_win\python.exe`（有 qtpy/PyQt6/numpy/cv2，**缺** docx/docx2txt/piexif/ordered_set）。
- 已装补齐依赖到 `D:/ruanjian/up_deps`（python-docx、docx2txt、piexif、ordered_set）。测试命令模板：
  ```
  cd <worktree> && PYTHONPATH=D:/ruanjian/up_deps <fork-python> -u -c "...sys.path.insert(0,'.'); sys.path.insert(0,'D:/ruanjian/up_deps'); os.environ['QT_QPA_PLATFORM']='offscreen'..."
  ```
- **offscreen 测试两个坑**：① MainWindow 退出时段错误 → 结尾 `os._exit(0)`；② 输出可能被吞 → 用 `-u` + `flush=True`。
- 徽标渲染测试模板：构造 `TextBlock`（`xyxy` + `lines=np.array([[0,0,200,0,200,60,0,60]],dtype=np.float32)` + `translation`），`TextBlkItem(blk, idx=2)` 加到**普通 QGraphicsScene**（Canvas 场景渲染矩形有坑），`scene.render` 到 QImage 数像素；DeviceCoordinateCache 记得每轮 `item.update()`。
- i18n 验证：fork 脚本 `scripts/qm_compile.py` 临时编译 `.qm`，`QTranslator.load` + `app.translate(context, source)` 校验（**验证完不要提交 .qm**）。
- 语法检查：`py_compile.compile(f, doraise=True)`。

## 七、坑与注意（务必读）

1. **pt_BR.ts 是 6 空格缩进**，其他语言 4 空格——任何按缩进正则改 ts 的脚本在 pt_BR 上会失败（已踩过：功能 1/2 的脚本都漏了 pt_BR，已在 reorder 分支和 shortcuts 分支修复，见下）。
2. **快捷键 i18n 的 tr() context 必须匹配调用类**：动作名/占位符/提示在 `_ShortcutRow` 类里 `self.tr()` → ts context 必须是 `_ShortcutRow`；组名在 `ShortcutEditor` → `ShortcutEditor`。放错 context 运行时显示英文（已修正）。
3. **PR 只提交 `.ts`，不提交 `.qm`**（作者明确 .qm 随正式版发布更新）。
4. 上游禁止硬编码中文，全部 `self.tr("English")` + ts。
5. 提交消息风格：`feat: <一句话> (source: BallonsTranslator-lite)`，英文。
6. 一次一功能一分支；功能 3 分支含功能 1/2 提交，推 PR 时**先推功能 1（快捷键）让上游先合并**，GitHub 会自动把已合并提交从功能 3 的 diff 排除（PR 描述里注明依赖）。
7. 不新增依赖、不整文件覆盖上游同名文件（先 diff 搬增量）、不改模块注册 key、不碰 `userspace_update` 工作区。
8. 上游 `.ts` 冲突修复脚本是一次性临时脚本（fix_ts_conflicts / fix_ts_conflicts2 / fix_ptbr / fix_ptbr_sc，当时放系统 /tmp 目录，仅作参考，无留存价值）。
9. 完成每个功能后回填 `docs/技术实现/反向移植_规范.md` §10 记录表；fork 侧收尾（daily_log）由用户在 fork 确认后做。

## 八、下一步建议（待用户操作：推送与开 PR）

四项功能逻辑已实机验证通过（用户 2026-08-17 审查，无功能 bug）。**唯一遗留：功能1 快捷键页 UI 风格适配（见第三节已知问题），标注需处理，推 PR 时在 PR 描述里注明。** 推送/开 PR 需用户执行（需要用户 GitHub 权限；当前克隆 `origin`/`upstream` 直指 dmMaze 只读，`myfork` remote 已配置指向 lite fork 仓库）：

0. **（可选，不影响逻辑）处理功能1 UI 适配**：把快捷键页样式改造成上游 `PanelGroupBox`/frameless 风格，或在 PR 描述中注明交由上游作者按己方风格调整。
1. **rebase 对齐（推前必做）**：把 `feat/backport-path-reorder` rebase 到最新 `feat/backport-shortcuts`（`ec47709`），替换掉 merge 进来的旧 `3d00008`，避免 shortcuts PR 合并后 reorder diff 出重复补丁。同时重跑功能3 冒烟脚本确认内容无损。
2. **按依赖顺序推 PR**：先推 shortcuts（功能1，PR 描述注明快捷键页 UI 需作者按上游风格调整）让上游先合并 → seq-badge（功能2）→ reorder（功能3，PR 描述注明依赖前两者）→ click-deselect（功能4，独立）。
3. 推送命令（在各 worktree 内执行，代理必须）：`git -c http.proxy=http://127.0.0.1:7897 push myfork <分支>`。
4. 推送后用 compare API 校验 diff（仅含自己的提交、behind 0），再在浏览器提 PR（base=dev，描述中文）。
5. 收尾：PR 状态变更后回填规范 §10；fork 侧 daily_log 由用户确认后写。
