# 文本引擎移植 · 阶段 0 进度（前置准备）

> 阶段范围：`docs/技术实现/文本引擎架构跟进.md` 路线图中的阶段 0
> 完成日期：2026-08-04
> 状态：完成

## 问题 / 需求

阶段 0 是移植工作的前置准备：把上游 v1.5.9 的差异基线建立起来，修正跟进文档中的事实错误，
为后续阶段的逐文件移植提供可核对依据。

## 改动要点

1. **拉取上游 tag**：`git fetch upstream-tmp --tags` 成功，tag `v1.5.9` 已在本地。
   注意：本 fork 的上游远端名是 `upstream-tmp`，不是跟进文档原先写的 `upstream`。
2. **建立 v1.5.8 → v1.5.9 diff 基线**（`git diff v1.5.8 v1.5.9` 核对）：
   - `ui/text_engine/` 包**全新增**：35 个文件，约 19683 行
     （item/layout/geometry/effect_renderer/shape_control + editing/、formatting/、rendering/、transforms/ 四个子包）。
   - `utils/fontformat.py`：+691 / -3（TextTransform 类族与持久化改造）。
   - `utils/proj_imgtrans.py`：+6（`TextBlkEncoder` 改走 `to_serializable_dict()`）。
   - 删除旧文件：`ui/textitem.py`(-1150)、`ui/scene_textlayout.py`(-1100)、
     `ui/texteditshapecontrol.py`(-346)、`ui/text_graphical_effect.py`(-28)，共 2626 行。
   - `utils/textblock.py`：**零变更**——上游变换机制全部挂在 `FontFormat` 上，`TextBlock` 模型未动。
   - 相关资源：新增 `doc/ui/text_engine.md`(+226)、`doc/ui/text_transforms.md`(+405)、
     4 个变换图标；`tests/test_text_transform_undo.py`(+4478) 为主测试。
   - 依赖：上游新增 `numba>=0.58.1` 与 `einops`（grid 变换用）。
3. **勘误跟进文档**（`docs/技术实现/文本引擎架构跟进.md`）：
   - 远端名 `upstream` → `upstream-tmp`，补充"tag 已 fetch"的事实修正。
   - 阶段 0 章节追加完整 diff 基线清单表。
   - 追加一个决策结论：**暂不引入**上游 `TEXT_TRANSFORM_SCHEMA_VERSION` 迁移方案，
     采用本地 AGENTS.md 容错策略（非法条目逐个丢弃并告警）。

## 涉及文件

- `docs/技术实现/文本引擎架构跟进.md`（勘误 + 基线清单）
- 上游基线读取（不改动）：`git fetch upstream-tmp --tags`、`git diff v1.5.8 v1.5.9`、
  `git show v1.5.9:<path>`

## 排障备忘

- 后续阶段需要对照上游代码时，用 `git show v1.5.9:<path>`，不需要重新 fetch。
- 上游文本引擎代码行号以 tag `v1.5.9` 为准；本地文件行号以工作区为准。
- 若 `git fetch upstream-tmp --tags` 在新机器上未执行过，先执行再对基线。

---

## 阶段 1 衔接说明

阶段 1（模型层）已按此基线的 fontformat.py 上游版本移植，进度见
`docs/技术实现/文本引擎移植_阶段1_模型层.md`。
