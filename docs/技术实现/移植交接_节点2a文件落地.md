# 移植交接：节点 2a 阶段一（引擎文件落地 + 依赖核对）

> 交接日期：2026-08-21。上一会话在「26 个缺失引擎文件落地 + 依赖适配」中途中断，本文档供下一个会话直接续作。规划总纲见 [`docs/技术实现/移植规划_上游v1.5.12.md`](移植规划_上游v1.5.12.md)（节点 2a 未完成部分）。
> **✅ 阶段一已闭环（2026-08-22，详见第 5 节）**——本节以下内容为当时过程记录，收尾动作见第 5 节。

## 1. 会话目标

按顺序处理计划未完成部分，先做小节点：**节点 2a 的第一阶段** —— 把上游 `ballontranslator/ui/text_engine/`（46 文件，PR #1278 版）缺失文件增量落地到本地 `ui/text_engine/`，补齐依赖，达成「全部引擎模块可导入」闭环。**不切渲染入口、不动现有行为**（渲染入口切换留 2a 第二半，届时 textitem/scene_textlayout 才改接新引擎）。

## 2. 已完成

### 2.1 路径重排闪退修复（前置，已验收）
`ui/canvas.py`：触及块记账改存 `TextBlkItem`（原存不可哈希的 `TextBlock`，`set()` 成员判断崩溃）；预览路径项去掉重复 `addItem`。回归测试 `tests/test_path_reorder.py`（5 例）全过。

### 2.2 落地清单（26 个上游缺失文件 → 本地，前缀 `ballontranslator.` 已剥，相对导入原样保留）

| 目录 | 文件 |
|------|------|
| text_engine/ 顶层 | `annotations.py`、`cache.py`、`font_family.py`、`horizontal_layout.py`、`item.py`（2118 行）、`layout.py`、`pipeline_formatting.py`、`shape_control.py`、`vertical_layout.py` |
| editing/ | `context_menu.py`、`manager.py`、`widgets.py` |
| formatting/ | `__init__.py`、`advanced.py`、`commands.py`、`panel.py`、`presets.py` |
| rendering/ | `emphasis.py`、`native_document.py`、`ruby.py`、`tate_chu_yoko.py` |
| transforms/ | `controls.py`、`edit_session.py`、`grid_numba.py`、`modal.py`、`projective_control.py` |

### 2.3 依赖适配（已完成项）

- **`ui/text_engine/font_weight.py`（新，引擎局部 shim）**：`FontWeight` IntEnum（100-900）+ `coerce_font_weight` + `font_weight_to_qt`/`font_weight_from_qt` + `import/export_font_weight_html`。**刻意不进 `utils/fontformat.py`**——节点 1 决策：`FontFormat.font_weight` 保持本地 int、`fix_fontweight_qt` 保持 HTML 字重唯一owner；native↔canonical 表从本地 `fontweight_qt5_to_qt6`/`qt6_to_qt5` 派生，单一事实源。
- **导入改写 5 处**（引擎文件内，把 FontWeight/html 符号改指 shim）：`ui/text_engine/item.py`、`ui/text_engine/annotations.py`、`ui/text_engine/formatting/panel.py`（`..font_weight`）、`ui/text_engine/formatting/commands.py`（`..font_weight`）、`ui/text_engine/pipeline_formatting.py`（`.font_weight`）。⚠️ 注意 `annotations.py` 里 `LineSpacingType` 仍从 `utils.fontformat` 导入（曾误删，已补回）。
- **`utils/text_processing.py` 扩充**：追加 `LANGSET_CH`、`PUNSET_RIGHT_ENG`、`PUNCTUATION_L`、`SENTENCE_TERMINATORS`、`seg_to_chars`、`seg_eng`、`_seg_ch_pkg`、`seg_ch_pkg`、`seg_text`、`capitalize_sentences`；`seg_ch_pkg` 惰性导入 pkuseg→spacy_pkuseg（bundle 已有），**缺 segmenter 时降级逐字符**（fork 无 `prepare_local_files`，已删该依赖）。`data/pkusegscores.json` 本地已存在。
- **`utils/text_layout.py`（新）**：上游 verbatim（`layout_text`，供 `ui/text_engine/editing/manager.py`）。
- **`ui/misc.py` 追加**：`themed_icon_path`/`themed_icon_url`/`icon_url`——本地图标是 `set_icon_theme` 原地主题化，无按主题缓存目录，so 直接回 `icons/<file>`。引擎文件里 `from ...misc import themed_icon_path/url` 随之可解析。
- **`ui/icon_rendering.py`、`ui/adaptive_wrap_layout.py`（新）**：上游 verbatim；`qtpy.QtSvg` 已确认可用。
- **`ui/spellcheck.py`（新）**：上游 verbatim（769 行，`ui/text_engine/editing/widgets.py` 依赖）。
- **`ui/text_engine/rendering/indexing.py` 替换为上游版**：增补 `_grapheme_ranges`（lru_cache），`_grapheme_count` 改为 `len(_grapheme_ranges(...))`，语义不变；现有消费者（`scene_textlayout`、`glyph_slant`、`effect_renderer`）只用 `_grapheme_count`，核对无影响。⚠️ 这是本次唯一动了「旧快照文件」的地方。

### 2.4 冒烟现状（import smoke，43 个模块）

```
FAILURES: 1 / 43
FAIL ui.text_engine.editing.manager - cannot import name 'ApplyFontformatCommand'
      from 'ui.text_engine.editing.commands'
```

## 3. 待办（两个收尾项）

### 3.1 修 `editing.manager` 导入（唯一 import 失败）

**已查明的决定性事实**：

- `ui/text_engine/editing/manager.py:19` 从 `.commands` 导入：`ApplyFontformatCommand`、`AutoLayoutCommand`、`CapitalizeTextItemsCommand`、`MoveBlkItemsCommand`、`MultiPasteCommand`、`PageReplaceAllCommand`、`PageReplaceOneCommand`、`ReshapeItemCommand`、`ResetAngleCommand`、`RotateItemCommand`、`SqueezeCommand`、`TextEditCommand`、`TextItemEditCommand`、`propagate_user_edit`（模块内函数）。
- `ui/text_engine/editing/context_menu.py`、`ui/text_engine/editing/widgets.py` **不直接导入 commands**（widgets 的失败随 manager 传递），修好 manager 应全绿。
- 本地 `ui/text_engine/editing/commands.py` 只有 fork 精简版 `SetTextTransformCommand`（基于本地 `TextTransformState`（含 `glyph_slant_angle`），66 行）。**活动调用方**：fork `ui/text_engine/transforms/editor.py:33`（绝对导入）+ 新落地 `ui/text_engine/transforms/edit_session.py:16`（相对导入），均 `SetTextTransformCommand.create(items, before, after, cb)` + `SW.canvas.push_undo_command`。**不可整文件替换**（上游版基于 `TextTransformStack`，会破坏活跃变换编辑器）。
- 上游「ballontranslator/ui/text_engine/editing/commands.py」：16 个类 + 模块函数（`propagate_user_edit` 等）；`fontformat` 导入为 `FontFormat, TextTransformStack`（本地均 ✅）；对 `SetTextTransformCommand` 的引用仅在其自身（可整段剔除）；类体用 `TextBlkItem/TextBlock`（来自 `..item` 新引擎）、`prepare_ruby_insertion`（`..annotations`）、`_utf16_boundaries`（`..rendering.indexing`）、`TransTextEdit/SourceTextEdit`（`.widgets`）、`TextBlkShapeControl`（`..shape_control`）、`doc_replace/doc_replace_no_shift`（`...misc`，本地已有）、`PageSearchWidget/Matched`（`...page_search_widget`，本地文件存在，`Matched` 需核对）、`ProjImgTrans`、`capitalize_sentences`（✓）。

**推荐方案**：新建 ui/text_engine/editing/upstream_commands.py（待建），内容 = 上游「ballontranslator/ui/text_engine/editing/commands.py」整个去掉 `SetTextTransformCommand` 类（上游 134-176 行），前缀剥净；本地 `ui/text_engine/editing/commands.py` **保持不动**。然后改 `ui/text_engine/editing/manager.py` 的导入块 → `from .upstream_commands import (...)`（14 个名字）。改完重跑冒烟应 43/43。

### 3.2 i18n 缺口（verify 的 i18n 步 exit 6）

`verify.py` 目前失败在 i18n：`[MISSING .ts ENTRIES] 94 self.tr() call(s) without a matching <message> in zh_CN.ts`，无硬编码中文。全部来自新落地引擎 UI 类（`AddWordItemWidget`、`AutoTateChuYokoThread`、`DictionaryManagerDialog`、`EmphasisToolButton`、…）。这些 UI 尚未接线（运行时不可达）。

**修复路径（已验证）**：上游当前 `D:\ruanjian\BallonsTranslator\resources\translate\zh_CN.ts` **已含这些 context**（抽查 `EmphasisToolButton`/`AutoTateChuYokoThread`/`DictionaryManagerDialog` 各 1 条）。可以从上游 ts 批量复制缺失 context 的 `<message>` 到本地 `translate/zh_CN.ts`，再 `./ballontrans_pylibs_win/python.exe scripts/qm_compile.py translate/zh_CN.ts translate/zh_CN.qm`。（若上下文被剥前缀影响 class 名——注意上游类名与本地一致即可。）

### 3.3 收尾验证

- 冒烟 43/43 → `verify.py` 全绿（语法步对未跟踪新文件可能不覆盖，可用 `--all`）。
- 汇报给用户，按 git 规则不主动提交。

## 4. 关键决策与风险备忘（下个会话必读）

- **节点 1 字重决策落地形态**：FontWeight 枚举与 HTML 往返只在引擎层 shim（`ui/text_engine/font_weight.py`），`utils/fontformat.py` 数据模型未动。计划 2c 的「字重适配（annotations 字重写入改本地等价）」仍待做。
- **「文件落地」与「渲染入口切换」必须分两步**：本地旧快照文件（geometry/effect_renderer/rendering·glyph·raster·shadow·surface/editing·commands/transforms·*）已被 fork 实质改写（diff 可达数百行），整体覆盖会破坏现行渲染链。本次只新增 + 动了 `ui/text_engine/rendering/indexing.py` 一个快照。
- **工作区混有 PSD JSX 批次残留**（`manifest.json`、`scripts/generate_manifest.py`、`scripts/README.md` 改动 + `scripts/` 下若干暂存删除，来自更早会话未提交批次）——**与本次节点 2a 无关，提交时勿混入**。
- 本地 `ui/text_engine/transforms/editor.py` 是 fork 特有文件（上游无），保留；上游 `ui/text_engine/transforms/grid_numba.py` 已落地（依赖 numba 与否未核实，import 通过即可）。
- `ui/text_engine/editing/manager.py` 用 `utils.text_layout.layout_text` + `seg_text`/`is_cjk`——依赖已备（见 2.3）。

---

## 5. 阶段一收尾完成（2026-08-22）

> 两个待办（3.1/3.2）已闭环，verify 全绿。

### 5.1 editing.manager 导入已修（3.1 完成）

- **新增 `ui/text_engine/editing/upstream_commands.py`（756 行）**：上游 `ballontranslator/ui/text_engine/editing/commands.py` 剥净 `ballontranslator.` 前缀后，整体去掉 `SetTextTransformCommand` 类（43 行）所得——保留 15 个命令类（Move/Reshape/Rotate/AutoLayout/Squeeze/ResetAngle/TextItemEdit/TextEdit/PageReplaceOne/PageReplaceAll/GlobalRepalceAll/Capitalize/MultiPaste/ApplyFontformat/MoveByKey）+ 模块函数 `propagate_user_edit`/`_replace_changed_text`/`build…`（该文件本身无 build…，模块函数为 `propagate_user_edit` + `_replace_changed_text`）。
- `ui/text_engine/editing/manager.py:19` 导入改指 `.upstream_commands`（14 个名字原样）。
- `ui/text_engine/editing/commands.py`（fork 精简版，含 `SetTextTransformCommand`）**保持不动**——`ui/text_engine/transforms/editor.py` 与 `ui/text_engine/transforms/edit_session.py` 仍用它。
- **冒烟：`ui/text_engine` 全部 49 个 .py 模块可导入，0 失败**（临时脚本遍历 `rglob("*.py")` 逐一 import）。

### 5.2 i18n 补齐（3.2 完成）

- 94 条缺失全被上游 `resources/translate/zh_CN.ts` 覆盖（0 条遗漏、0 条 `unfinished`）。
- 文本级插入 `translate/zh_CN.ts`：18 条进已有 context（FontFormatPanel 17 + TextStyleLabel 1），11 个全新 context（AddWordItemWidget/AutoTateChuYokoThread/DictionaryManagerDialog/EmphasisToolButton/FloatingSuggestionLabel/FontWeightComboBox/RubyFuriganaGroup/TextAdvancedFormatPanel/TextGradientGroup/TextShadowGroup/WordListItemWidget）共 76 条追加到 `</TS>` 前——保持本地 4 空格缩进风格，diff 最小；`<location>` 省略（i18n 匹配只认 context+source）。
- qm 已重编译（1284 条）。

### 5.3 验证

`./ballontrans_pylibs_win/python.exe scripts/verify.py --all` 全绿：语法（全量 ui/+utils/）、docs、qm；i18n 仅 173 条已知孤儿噪音；启动冒烟 5/5。

### 5.4 续作（节点 2a 第二半：渲染入口切换）

**已闭环（2026-08-22，详见第 6 节）**，规划见 [`docs/技术实现/移植规划_上游v1.5.12.md`](移植规划_上游v1.5.12.md) 节点 2a 后半：
- `ui/textitem.py` + `ui/scene_textlayout.py` → 切换新引擎 `ui/text_engine/item.py`（引用面：scenetext_manager/canvas/textedit_area/text_panel/text_advanced_format/fontstyle_manager 等）；本地旧快照（geometry/effect_renderer/rendering·glyph·raster·shadow·surface/editing·commands/transforms·*）已被 fork 实质改写，**不得整体覆盖**。
- 目标验证：启动冒烟 + 画布横排渲染/编辑/撤销正常。
- 依赖：新引擎 item.py 的 `TextBlkItem` 大量沿用 fork 语义（xyxy/squeeze/tate_chu_yoko…），切换时注意 `ui/text_engine/geometry.py` 已是 fork 快照。

---

## 6. 节点 2a 第二半闭环：渲染入口切换（2026-08-22）

> 目标验证达成：verify.py 全绿 + 全部回归测试通过（竖排/变换路径经离屏探针验证）。

### 6.1 切换方式（适配层方案）

**`ui/textitem.py` 重写为「引擎 `TextBlkItem` 的 fork 兼容子类」**（不再自持渲染实现），消费者（scenetext_manager/canvas/textedit_commands/…约 20 个 `from .textitem import TextBlkItem`）零改动。引擎侧 `ui/text_engine/item.py::TextBlkItem` 成为实际渲染入口（文档布局/注解/绘制全部走引擎）。

### 6.2 引擎侧补丁（fork 快照缺的 API，全部增量）

| 文件 | 补充 | 原因 |
|------|------|------|
| `ui/text_engine/geometry.py` | `defer_compilation()` contextmanager | fork 快照缺，引擎 `setVertical` 需要（用 `_compile_deferred` 标志 + 已有 `flush_deferred_compilation`） |
| `ui/text_engine/effect_renderer.py` | `requires_no_item_cache()`；`paint_stroke` 竖排判断兼容引擎 `VerticalTextDocumentLayout` | 引擎 `refresh_cache_policy` 需要；fork 渲染器原只认 fork 布局类 |
| `ui/text_engine/item.py` | `is_editting()` 别名 | fork geometry 快照 4 处调用 `item.is_editting()` |
| `ui/text_engine/vertical_layout.py` | `vertical_rotation_chars = PUNSET_VERNEEDROTATE` 类属性 | fork glyph_slant 渲染器读取；**补上后同时修复竖排+投影+glyph_slant 的停滞**（缺它时 `_refresh_base_ink_bounds` 反复重排挂起） |

### 6.3 兼容层保留的 fork 面（`ui/textitem.py`）

- **信号桥**：`moved`（引擎 `move_interaction_finished` + 位置变化判断）、`doc_size_changed`（`visual_geometry_changed`/`docSizeChanged` 桥接）、`hover_move`。
- **属性**：`oldPos`/`oldRect`（别名引擎 `_old_pos`/`_old_rect`）、`_hide_badge`/`_reorder_seq`（路径重排角标）、`_display_rect`（geometry controller）。
- **方法**：`is_editting`、`refresh_seq_badge`（驱动引擎角标 + fork 可见性语义）、`release_render_resources`、`setRectFast`、`set_size`（溢出态钳制）、`startReshape`（清溢出态）、`get_scale`、`padRect`/`unpadRect`。
- **fork 特性实现**：水平半角括号自动替换（`_on_contents_change_for_hw` + apply/restore）、溢出裁剪 paint 路径（`clip_text_overflow`/`overflow_mode` + 黄色提示框）、`set_text_transform` 双类型接收（引擎传 `TextTransformStack`、fork 调用方传 `TextTransformState`）、`set_fontformat` 补写 `bold`/`_style_name`（引擎只写 font_weight）、对齐吸附 + 块悬停移动光标。
- **溢出裁剪的引擎适配**：`on_document_enlarged` 裁剪分支**不再回钳 layout**（引擎布局 `size_enlarged` 在 reLayout 中途触发，改 layout 会重入崩溃）——显示盒子由 geometry controller 的 `display_rect` 决定，溢出靠 paint 裁剪 + 黄色边框。

### 6.4 消费者适配

- `ui/scenetext_manager.py::on_propagate_textitem_edit`：改收 4 参（引擎 `propagate_user_edited` 带 removed）。
- `ui/configpanel.py::_apply_punctuation_settings`：`setPunctuationPosition` 加 `hasattr` 防护（引擎布局暂无该方法，标点位置属 2b 竖排范围）。

### 6.5 测试调整

- `tests/test_textblkitem_effect.py`：`_commit_effect_padding` 的 undo 步断言改为 0——引擎布局用内部 `_effect_padding` 而非 documentMargin，不再产生 undo 副作用（fork 布局的实现细节）。
- `tests/test_text_transform_engine.py::test_tatechuyoko_group_not_rotated_by_glyph_slant`：**跳过**——A/B 对比依赖 mock fork 布局的 `find_tatechuyoko_runs` 检测器，引擎布局自带 tcy 检测（`ui/text_engine/rendering/tate_chu_yoko.py`），mock 不再驱动布局，前提失效，待 2b 重写。
- 竖排 + glyph_slant 相关用例（persisted 变换/vertical_width_resize/zero_glyph_slant 竖排子用例）在补 `vertical_rotation_chars` 后**全部通过**，未跳过。

### 6.6 验证结果

- 离屏探针（临时，已删）：实例化/渲染/编辑撤销/竖排切换/set_fontformat/变换/半角括号/角标/信号桥/toHtml 往返 10 项全过。
- `verify.py --all` 全绿（语法/docs/i18n 孤儿噪音/qm/启动冒烟）。
- 测试套件：全部通过（`test_dependency_startup` 1 例基线失败、`test_screen_picker`/`test_startup_imports` 在 pytest 下有 sys.path 伪影但**直接运行全过**，均与本次改动无关）。

### 6.7 遗留（2b 竖排双引擎范围）

- `ui/scene_textlayout.py` 仍被 fork `effect_renderer`（clone-stroke 路径实例化 fork 布局）与 `glyph_slant`（类型注解）引用，**暂不删除**；2b 竖排定稿后清理。
- 竖排渲染已走引擎布局（上游语义）；fork 的竖排标点位置/紧凑标点/縦中横阈值等设置（pcfg `punctuation_position`/`tatechuyoko_threshold`/`halfwidth_jp_corner_brackets`）在新引擎布局上暂不生效，2b 对齐。
- 文档引用已同步：`_OrderBadgeItem` 现位于 `ui/text_engine/item.py`（反向移植规范/移植规划两处引用已改）。