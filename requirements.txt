# ==============================================================================
# Project dependencies -- installed by launch.py via pip install -r requirements.txt
# Canonical declaration: pyproject.toml -> [project].dependencies (PEP 621).
# Heavy model-inference packages are NOT listed here; install what you need:
#   torch / torchvision / transformers / diffusers / ultralytics
# ultralytics (YOLO text detectors) is declared per-module via
# requires_packages, so the app installs it on demand when you pick such a
# detector.  See pyproject.toml [project.optional-dependencies].
# ==============================================================================
# GPU PyTorch: https://pytorch.org/

packaging>=23.0
pydantic>=2.0
pillow>=10.0
PyQt6-Qt6>=6.8.1
PyQt6>=6.8.1
numpy
# Local models need PyTorch, installed separately:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# YOLO detectors additionally need ultralytics:
#   pip install ultralytics
#   pip install transformers diffusers
# numba (optional, effect-render acceleration; falls back to pure NumPy):
#   pip install numba
tqdm
opencv-python>=4.10.0.84; sys_platform == 'win32' or sys_platform == 'linux' or sys_platform == 'darwin'
shapely
pyclipper
einops
fonttools
spacy-pkuseg
termcolor
qtpy
opencc-python-reimplemented
requests
colorama
openai>=2.8.1
pyyaml
natsort
networkx>=3.0
py7zr
httpx[socks,brotli]
pillow-jxl-plugin>=1.3.4
tomli; python_version < '3.11'
pywin32; sys_platform == 'win32'
