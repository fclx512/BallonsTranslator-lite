# ==============================================================================
# Project dependencies -- installed by launch.py via pip install -r requirements.txt
# Canonical declaration: pyproject.toml -> [project].dependencies (PEP 621).
# Optional GPU packages (torch, transformers, etc.) are listed in
# pyproject.toml [project.optional-dependencies].gpu -- install those manually.
# ==============================================================================
# GPU PyTorch: https://pytorch.org/

packaging>=23.0
pydantic>=2.0
pillow>=10.0
PyQt6-Qt6>=6.6.2
PyQt6>=6.6.1
numpy
# PyTorch and its dependencies are optional.
# Install them manually for GPU-accelerated local models:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#   pip install transformers diffusers
ultralytics>=8.4.14
tqdm
opencv-python>=4.8.1.78; sys_platform == 'win32' or sys_platform == 'linux'
shapely
pyclipper
einops
termcolor
qtpy
spacy-pkuseg
opencc-python-reimplemented
click
requests
colorama
openai>=2.8.1
pyyaml
natsort
networkx>=3.0
py7zr
httpx[socks,brotli]
pillow-jxl-plugin>=1.3.4
pywin32; sys_platform == 'win32'
